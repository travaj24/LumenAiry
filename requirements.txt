# Runtime dependencies for lumenairy
#
# Canonical source: pyproject.toml [project.dependencies] and
# [project.optional-dependencies].  This file mirrors the hard
# dependencies + optional groups for users who prefer
# ``pip install -r requirements.txt``.  v4.16.2 audit closure
# (P1-NEW-F2-HIGH-2): refractiveindex moved to the [glass] extras
# group; zarr bumped to >=3.0 to match storage.py's Group.create_array
# usage.
#
# === Required (core functionality will not work without these) ===
numpy>=1.20
scipy>=1.7                # resample_field (Fresnel grid resampling), optimization
matplotlib>=3.5           # plotting (spot_diagram, ray_fan_plot, plot_field, etc.)
# v4.16.3 (audit P3-NEW-F1-5): psutil is a hard dep in pyproject.toml
# [project.dependencies]; the v4.16.2 "Recommended" placement was a
# requirements.txt-vs-pyproject drift (the package does not crash on
# import without psutil, but `memory_info()` silently falls back to a
# 4 GB default which can mis-size batch allocations).  Promoted to
# Required for parity with pyproject.toml.
psutil>=5.0               # accurate memory detection (silent 4 GB fallback if absent)

# === Optional (uncomment as needed -- mirrors pyproject.toml extras) ===
# h5py>=3.0               # HDF5 field storage (save_field_h5, save_planes_h5, etc.); [hdf5] / [gui] extras
# pyfftw>=0.13            # multi-threaded CPU FFT (opt-in via USE_PYFFTW = True); [fft] extra
# numexpr>=2.8            # fused phase-screen multiply in apply_real_lens (1.5-2x speedup at large N); [perf] extra
# numba>=0.58             # JIT fastpath for _Cheb2DEvaluator in apply_real_lens_traced (newton_fit='polynomial'); [numba] extra
# cupy>=11.0              # GPU acceleration (CUDA / ROCm); [gpu] extra
# astropy>=5.0            # FITS file I/O for load_fits_field / save_fits_field; [fits] extra
# zarr>=3.0               # Zarr storage backend; v4.16.1 bumped from >=2.14 to match Group.create_array; [zarr] extra
# refractiveindex>=1.0    # extended glass-catalog lookups (Hikari, Sumita, formula-3 CDGM); v4.16.1 moved to [glass] extras
# filelock>=3.0           # distributed Zarr / HDF5 SWMR write lock; [zarr] / [hdf5] extras
# jax>=0.4.20             # automatic differentiation; [jax] extra
# pymoo>=0.6              # multi-objective NSGA-II; [multi_objective] extra
# PySide6>=6.5            # LumenAiry Designer GUI (also requires pyvista, pyvistaqt, h5py); [gui] extra
