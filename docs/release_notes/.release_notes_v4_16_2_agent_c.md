# v4.16.2 Agent C release notes

**Scope:** pre-v5.0 prep features (formula-3 evaluator + 3 default-config knobs) + 1 audit P3 in glass.py.

## Feature 1 -- Sellmeier formula-3 (polynomial) evaluator

`lumenairy/glass.py`:
* NEW `_polynomial_index(wavelength_m, coeffs, glass_name=None)` --
  refractiveindex.info formula-3 evaluator.  ``coeffs = (c0, [(c_i,
  exponent_i), ...])``; computes ``n^2 = c0 + sum_i c_i * lam_um ** exp_i``
  then ``n = sqrt(n^2)``.  Subsumes the Schott 6-coefficient
  polynomial form ``n^2 = a0 + a1*lam^2 + a2*lam^-2 + a3*lam^-4 +
  a4*lam^-6 + a5*lam^-8``.
* NEW `POLYNOMIAL_COEFFICIENTS = {}` -- empty at ship.  v4.16.2 lands
  the evaluator + dispatch wiring; populating the 26 catalogue
  entries (Hikari E-/J-, Sumita K-, 4 CDGM polynomial) requires
  vendor-source review against refractiveindex.info YAML at 5e-5
  n_d tolerance and is staged for v5.0.
* `get_glass_index` dispatch updated -- when refractiveindex is
  unavailable AND the glass is in POLYNOMIAL_COEFFICIENTS, dispatches
  to `_polynomial_index` before raising ImportError.

## Features 2-4 -- 3 default-config knobs

`lumenairy/propagators/propagation.py` -- 3 NEW setter/getter pairs
parallel to the existing `set_default_complex_dtype`:

* `set_default_real_dtype(dtype)` / `get_default_real_dtype()` --
  accepts ``np.float32`` or ``np.float64``; raises on others.
* `set_default_wave_propagator(name)` / `get_default_wave_propagator()`
  -- accepts `'asm'`, `'sas'`, `'fresnel'`, `'rayleigh_sommerfeld'`,
  `'rs'`; raises on unknown names.
* `set_default_dy(value)` / `get_default_dy()` -- accepts `None` (the
  library default; means "match dx") or a positive finite float.

All 6 functions exported at top level via `lumenairy/__init__.py`.

**Consumer wirings**: representative single-site wiring landed in
`lumenairy/propagators/ensemble.py` (the no-input-dtype fallback path
honours `get_default_real_dtype()`).  The full library-wide rollout
of resolvers (replacing hardcoded `'asm'` / `np.float64` / `dx`
defaults across all entry points) is staged for v5.0 alongside the
file-split work.

## Audit P3-NEW-F1-4 -- numpy-scalar acceptance in GLASS_VALIDITY check

`lumenairy/glass.py`: the v4.16.1 well-formedness check at
`_check_glass_registry_consistency` rejected
``isinstance(np.int32(0), (int, float))`` (False on Python 3.10+).
Widened to accept `(int, float, np.integer, np.floating)` so users
registering custom glasses with numpy-scalar validity bounds don't
trip the check.  Non-numeric types (`str`, etc.) still raise.

Required adding `import numpy as np` at module scope (glass.py was
previously numpy-free; it used `_math.sqrt` and `float('inf')`).
The new import is paid for by the audit closure.

## Tests

`tests/unit/test_v4_16_2_agent_c.py` -- 24 tests across 5 classes:

| Class | Tests | Coverage |
|---|---|---|
| `TestPolynomialIndexEvaluator` | 5 | Constant, Schott form, negative-n^2 reject, dict layout, dispatch wiring |
| `TestSetDefaultRealDtype` | 5 | set/get for f64/f32, reject complex/f16, top-level export |
| `TestSetDefaultWavePropagator` | 5 | set/get for asm/fresnel, reject unknown, reject non-string, top-level export |
| `TestSetDefaultDy` | 6 | set/get for None/positive, reject negative/zero/inf, top-level export |
| `TestGlassValidityNumpyScalarAcceptance` | 3 | np.int32 ok, np.float32+64 mix ok, str rejected |

24/24 pass at v4.16.2 HEAD.

## Files touched

* `lumenairy/glass.py` -- added `_polynomial_index` + `POLYNOMIAL_COEFFICIENTS` + dispatch wiring + numpy import + numpy-scalar acceptance in consistency check
* `lumenairy/propagators/propagation.py` -- added 6 set/get functions + 3 module-level globals
* `lumenairy/propagators/ensemble.py` -- 1-line wiring (no-input-dtype fallback honours `get_default_real_dtype`)
* `lumenairy/__init__.py` -- exported the 6 new functions

## Sibling-gap sweep

* Numeric-isinstance pattern in glass.py: only the v4.16.1 check trips on numpy scalars; other isinstance checks use the existing `(int, float)` pattern correctly (they validate user input where Python scalars are the documented type).
* Default-config knob pattern: `set_default_complex_dtype` was the only pre-existing knob.  v4.16.2 lands the 3 parallel knobs.
* Formula evaluation: `_sellmeier_index` (formula 2) was the only bundled evaluator.  v4.16.2 lands `_polynomial_index` (formula 3); 6 other refractiveindex.info formulas remain refractiveindex-only.
