## Multi-agent physics audit response — full series

**4.11.0 closes the converged findings from three independent audit runs** (one external 8-agent audit, two internal multi-agent audits) of the v4.9.0 codebase. Across five patch releases (4.10.0 → 4.10.1 → 4.10.2 → 4.10.3 → 4.11.0) **~100 audit findings** were addressed. All 34 validation files (314 tests) pass.

### Highest-impact fixes

- **Mirror Seidel coefficients no longer zero.** Every reflective/catadioptric system silently reported "diffraction-limited" Seidel sums pre-4.11 because the mirror branch in `seidel_coefficients` updated ray heights but never wrote S1..S5. Fixed via the Welford form with n2 = -n1.
- **Exit-pupil radius** uses transverse magnification 1/D (was D, the angular magnification). Off by 1/D² for non-trivial post-stop systems.
- **Tilted-ASM band-limit** centred on the original-frame spectrum (FX + fx0). Pre-4.11 the default `bandlimit=True` killed the baseband DC and zeroed the propagated field for any non-trivial tilt.
- **Rayleigh-Sommerfeld kernel sign** flipped to the Goodman 3-43 form (1/r − ik). Coherent superposition of RS with ASM/Fresnel was 180° out of phase pre-4.11.
- **Richards-Wolf** adds the missing 1/√(cos θ) Jacobian and the −ikf/(2π)·exp(−ikf) prefactor. High-NA focal-plane amplitudes are now physical.
- **Coord-break order** matches Zemax PARM 6 default (decenter-then-tilt). Every imported folded design with the Zemax default now gets the correct frame transform.
- **`MultiWavelengthMerit`** actually re-propagates the wave leg per wavelength (was a no-op chromatic constraint pre-4.11).
- **`create_circular_polarized('right')`** returns the RHC Jones vector `(1, -i)/sqrt(2)` under the library's `exp(-i*omega*t)` convention.
- **JAX trace correctness floor**: sign(R) on sag derivatives, TIR double-where for finite `jax.grad`, `trace_jax` raises `NotImplementedError` on unsupported surface types instead of silently treating them as flat refractive.
- **HFPI** Kirchhoff weighting now includes the 1/(iλ) prefactor and solid-angle Monte-Carlo weight. Absolute amplitudes were unphysical by ~10⁶ pre-4.11.
- **Shack-Hartmann** drops the bogus `wavelength/(2π)` factor on wavefronts; reference-centroid calibration pass added.
- **`through_focus_scan_jax`** now uses `jax.vmap` over an inline ASM kernel (was a Python for-loop) — ~5-15× speedup, more on GPU.
- **`aberration_tensor` ℓ=0 closed-form** evaluates the output LG polynomial at the saddle's σ_image, distinguishing defocus from higher-order spherical for off-axis saddles.

### New / improved API

- `ChromaticFocalShiftMerit(wavelengths=[...])` is self-contained (no longer depends on `MultiWavelengthMerit` side-effect).
- `check_sampling_conditions(NA=...)` for relaxed Nyquist `λ/(2·NA)`.
- `register_fixed_glass` works without `refractiveindex` installed.
- All phase-retrieval functions accept `seed=` and `dtype=` for reproducibility / backend parity.
- Source factories (`create_top_hat_beam`, `create_annular_beam`, `create_bessel_beam`) accept `dy` for anamorphic grids.
- `apply_real_lens` respects decentered stop apertures.
- `apply_jones_matrix` validates callable-returned matrix shape.

### Documented limitations

- `_transfer_jax` retains the paraxial form `x += L·thickness`. The math-correct `t = (thickness − z)/N` introduces a gradient instability through `fit_canonical_polynomials_jax` whose root cause needs deeper investigation. Paraxial form is accurate to ~1% for NA ≤ 0.1 (the typical LumenAiry workflow).
- `aberration_tensor` axial multi-p ℓ=0 case emits a warning; saddle-point closed-form genuinely can't distinguish on-axis modes that all peak at the origin. Use off-axis saddle or include ℓ ≠ 0 modes.

### Methodology

This release was driven by three concurrent audit passes from multiple agents. Each finding has a file:line citation; full fix-by-fix details and the explicit list of items not addressed (with rationale) live in [CHANGELOG.md](https://github.com/travaj24/LumenAiry/blob/main/CHANGELOG.md).

🤖 Generated with [Claude Code](https://claude.com/claude-code)
