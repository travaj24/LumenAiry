## Round-3 fresh-eyes audit response

An 11-agent fresh-eyes audit of v4.11.1 (`AUDIT_ROUND3_2026_05_16.md`) surfaced ~120 new substantive findings. **v4.11.2 closes ~70 of the highest-impact findings across seven parallel work tracks plus a reconciliation sweep, and ships ~55 new pinning regression tests.** All 243 unit tests pass; full `validation/run_all.py` suite (34 files, 314 tests) passes.

### Headline meta-finding

**The v4.10 "C-LR-1 fix" was itself wrong.** The pre-v4.10 sign on the Seidel-correction OPL inside `apply_real_lens` was correct; the round-1 audit's physics-reasoning step that justified flipping it was reversed. v4.11.1's threshold tweak (50 nm → 5 nm) didn't help because the resulting bogus correction was millimetre-scale, not nm-scale. **v4.11.2 reverts the sign and pins it** with a ground-truth regression test that compares the analytic-screen path against `apply_real_lens_traced`. The comment block in the code now spells out the physics under the library's `exp(-iωt)` convention so this can't happen again.

This is the first instance in the audit series where the audit itself **introduced** a bug rather than catching one. It's a cautionary case for the discipline of *"every fix needs a regression test against ground truth"*.

### Critical-tier reversals and corrections

- **C-LR-1 reverted** — `_lens_real.py:867-895` — `opl_wave_rel = -(opl_analytic - opl_analytic[i_ax])` restored.
- **GBD `axial_opl` actually works now** — `gbd.py:569-606`. v4.11.1 called `.get('thickness', ...)` on `Surface` dataclass instances (no `.get` method); `AttributeError` was silently swallowed by a bare `except`. Switched to `getattr`; narrowed except emits a `RuntimeWarning`.
- **S-LAH64 / S-LAH79 Sellmeier coefficients removed** — `glass.py:172-182`. In-code coefficients gave `n_d` off by +5.8% and −5.9% vs the Ohara catalog (verified by direct evaluation). Both glasses now route through the `__sellmeier__` sentinel + refractiveindex.info lookup.

### Critical-tier physics fixes

- **Chained-mirror Seidel parity** — Cassegrain / Schwarzschild / any 2-mirror catadioptric system now produces correct Seidel sums beyond the first mirror.
- **`system_abcd` ↔ `seidel_coefficients` mirror sign agreement** — both use Welford signed-R now; for R = −100 mm concave mirror, EFL = +50 mm in both APIs.
- **`seidel_wfe` field-curvature DC term added** — `(1/4)·S₃·ρ²` was missing from both docstring and code.
- **EVENASPH PARM off-by-one in Zemax loader** — every Zemax-authored EVENASPH file ever loaded by Lumenairy had silently lost its α₄. Fixed.
- **Quadoa aspheric serializer** — was iterating dict keys, writing `[4.0, 6.0, 8.0]` (the powers) to JSON instead of the coefficients.
- **`normalize_prescription` mirror filter** — was a no-op (wrong dict key); fixed.
- **Zemax exporter STOP marker on folded designs** — was indexing by surface_counter (including coord-breaks + mirrors); now uses refractive-only counter.
- **Mirror / coord-break DISZ round-trip** — was applying a double-flip; removed.
- **`propagate_hfpi_through_prescription` finite-conjugate dead path** — paths were initialised at z=0 and immediately killed by `t≥0` mask after back-propagation. Now initialises at `z = −object_distance` directly.
- **`init_paths_stratified`** — was enumerating 2 of 16 strata; now full cartesian product.
- **Richards–Wolf prefactor** — `1/f²` factor and `exp(+ikf)` sign both restored; intensity now scales physically with focal length and global phase matches the rest of the forward propagators.
- **`compute_psf` Parseval default broke `t_strehl_perfect`** — test was passing for the wrong reason (peak ~89795 > 0.99); test updated to request `normalize='peak'` explicitly with `abs(peak − 1) < 1e-9`.

### High-tier sibling / dead-on-arrival fixes

- `propagate_huygens_fresnel_with_opl_callable` missing `-1j` Maslov prefactor (sibling of v4.10's `propagate_hf_chebyshev_quadrature` fix).
- `propagate_huygens_fresnel_through_prescription(method='asymptotic')` was silently replacing `E_in` with a fundamental Gaussian. Now decomposes via `decompose_lg`.
- HFPI Kirchhoff `1/(iλ)·dΩ` added to `apply_aperture_diffraction` + vectorial HFPI sites.
- HFPI RNG now spawns per-aperture child seeds (was reusing master seed → perfectly correlated draws).
- Asymptotic Maslov branch-tracking hoisted to shared helper; `aberration_tensor` (NumPy) routes through it.
- `maslov_tracking` kwarg on `propagate_modal_asymptotic` with `{'principal', '1d_raster', 'row_reset'}`; default `'row_reset'` fixes 2-D raster row-wrap.
- Subaperture per-patch fit centred via `source_centre` kwarg (was using a global fit).
- Real `E_in` dtype-promote in HF Chebyshev quadrature (was stripping kernel imag part).
- AO rim Zernike FD extended to all four rim quadrants.
- EP-aiming siblings: `relative_illumination`, `field_aberration_sweep`, `ray_fan_data`, `opd_fan_data` now aim chief at EP centre.
- Phase-retrieval `seed=`/`dtype=` parity actually wired through `gerchberg_saxton_jax`, `error_reduction`, `hybrid_input_output`.
- `apply_real_lens_traced` M_x / M_y indices fixed.
- Freeform terms in thin-element `apply_real_lens` emit a warning instead of silently dropping.
- `stop_index ≠ 0` in `apply_real_lens_traced` and `apply_real_lens_maslov` warn instead of silently ignoring.
- NaN sentinel leak from aspheric clamp masked before `exp(-i·k·NaN)`.
- Coating `'avg'` mode stores `eta_s`, `eta_p` separately per polarization.
- `apply_waveplate` docstring synced to actual implementation.
- `spot_diagram` / `trace_summary` Airy radius now includes `|f_eff|` factor.
- `bundles.py` conversion helpers rewritten to use real `RayBundle` attributes.
- `find_best_focus` NaN guard, `monte_carlo_tolerancing_linearized` clamps `a_k ≥ 0`, `compute_psf` non-square pupil clear error, `apply_detector` non-integer ratio area scale correction.
- codegen `op.GLASS_REGISTRY` → `la.GLASS_REGISTRY`; system-list style emits `aperture_diameter` for mirrors.
- `load_material` warns on dropped dispersion field.
- `Source.*` classmethods propagate `**factory_kwargs`.

### dtype hygiene

Remaining hard-coded `np.complex128` allocations replaced with `get_default_complex_dtype()` in `vector_diffraction.py`, `optimize/core.py` (`design_optimize.E0`), and `JonesField.__init__`. The `precision='single'` knob now propagates further through the wave-leg pipeline.

### Test-suite + harness improvements

- ~55 new pinning tests in `tests/unit/test_audit_fixes_v4_11_2_*.py` covering raytrace+Seidel, IO/Zemax/prescriptions, RW+lens, HFPI+HF+asymptotic, analysis, and Track A (C-LR-1 / GBD / S-LAH).
- Strengthened 3 v4.11.1 pinning tests that were passing for the wrong reason (MultiWavelengthMerit chromatic semantics; Subaperture actual call; Tilted-ASM tilt magnitude).
- New RS-vs-ASM phase pinning test — the single biggest test-coverage gap per the round-3 audit.
- `validation/_harness.py:33`'s blanket `warnings.simplefilter('ignore')` scoped to deprecation/resource/import categories only; `RuntimeWarning` / `UserWarning` / numerical-overflow warnings now propagate.
- Removed bare-`except: return True, 'skipped'` patterns in `t_dammann_grating`, `t_prescription_hf_asymptotic`, `t_subaperture_asymptotic_singlet` that were hiding genuine failures.

### Methodology

Round-3 audit used 11 parallel fresh-eyes audit agents over ~74 kLOC. v4.11.2 fix-side used 7 parallel work-track agents (one per physics domain) plus a manual reconciliation sweep. Each agent verified every finding against the actual code before fixing (rejected ~5 audit claims as not-reproduced after inspection). Per-finding evidence and file:line citations live in `AUDIT_ROUND3_2026_05_16.md`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
