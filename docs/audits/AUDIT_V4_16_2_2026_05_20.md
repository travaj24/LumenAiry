# Lumenairy v4.16.2 — Audit Report

**Date:** 2026-05-20
**Library state:** v4.16.2 at HEAD `9851f68`.
**Scope:** (a) verify v4.16.2 closes the v4.16.1 audit (`AUDIT_V4_16_1_2026_05_19.md`) — 5 P1 + 8 P2 + 9 P3 = 22 items; (b) audit v4.16.2's NEW pre-v5.0 prep features (bundled Sellmeier formula-3 evaluator + 3 default-config knobs + Migration-Guide.md + 11th meta-pin walker); (c) exhaustively scan for new bugs, physics correctness, regressions in modified code, and meta-pattern recurrence.

**Methodology:** 6 parallel single-domain auditors (V1-V4 verification + F1 fresh-eyes bug hunt + F2 cross-cutting). v4.16.2 is a focused closure + pre-v5.0 prep release: +5160 LOC additions / -251 LOC deletions across 26 files. Library-internal LOC dominated by `optimize/core.py` (+262 LOC; MultiWavelengthMerit FutureWarning + Constraint .validate() + pickle.dumps probe + LM override warning), `propagators/ensemble.py` (+159 LOC; backend-preserving rewrite), `propagators/propagation.py` (+126 LOC; 3 default-config knob setters/getters + module-level globals), `glass.py` (+98 LOC; formula-3 evaluator + POLYNOMIAL_COEFFICIENTS dispatch), `raytrace/jax_trace.py` (+78 LOC; duck-typed gate + hoisted eager probe). Plus +203 LOC `Migration-Guide.md` (NEW) + 5 new test files (+2291 LOC).

---

## Part 0 — TL;DR

**v4.16.2 is a substantively solid closure release with one persistent structural concern.** All 22 v4.16.1 audit items close cleanly (V1-V4 verified). The headline production-path integration test that v4.16.1 missed (the `_transfer_jax` high-NA RuntimeWarning unreachable in user flow) is now present and exercises the canonical `make_jax_ray_state(...) → trace_jax(...)` factory pipeline. The backend-preserving `propagate_ensemble` rewrite is **structurally enforced** (xp.zeros / xp.abs / xp.stack with no NumPy fallback in the per-realization loop). The 10th cache-registry meta-pin is properly hardened (rejects function-nested + `if False:` enrollment calls). The new 11th doc-consistency meta-pin walker has 7 passing tests AND V4 empirically validated all 6 surface scans actually fire on synthetic drift (not vacuous). Migration-Guide.md is a 203-line working document with substantive recipes per breaking change, not a stub. **Full test suite: 2270 passed + 5 skipped + 1 xfailed = 2276 collected, reconciling exactly to the per-agent breakdown** (A=18 + B=16 + C=24 + D=20 = 78 net; 2113 + 85 + 78 = 2276) — v4.16.1's 2208/+102/2106 arithmetic confusion is fully closed. Formula-3 evaluator math is **bit-exact** against hand-derivation (V3 verified `1.5169027204` for N-BK7 polynomial-form match).

**However, this audit identifies 2 NEW P1 + 6 NEW P2 + 8 NEW P3.** Most concentrate around the new pre-v5.0 prep features (default-config knobs that are net-negative scaffolding; Migration-Guide example that documents unimplemented behavior; V11 walker that contains the same structural-bypass pattern it was meant to retire).

**Headline new findings (sorted by severity):**

1. **🔴 P1-NEW-F1-1: Migration-Guide.md documents UNIMPLEMENTED behavior.** The v4.16.2 recipe at `Migration-Guide.md:163-175` shows:
   ```python
   la.set_default_wave_propagator('fresnel')
   field = la.apply_real_lens(field, prescription=pres, wavelength=wl, dx=dx)
   ```
   But `apply_real_lens` at `_lens_real.py:209` hardcodes `wave_propagator: str = 'asm'` and **does NOT consult `get_default_wave_propagator()`**. A user copy-pasting this recipe silently uses ASM instead of Fresnel — silent numerical-answer change. The later paragraph hedges, but the code example already misled.

2. **🔴 P1-NEW-F1-2: The 11th walker's pyproject regex silently mis-parses nested brackets.** The non-greedy regex `r'^([a-zA-Z_][a-zA-Z0-9_-]*)\s*=\s*\[(.*?)\]'` at `test_v4_16_2_dispatcher_pin_doc_consistency.py:75-77` stops at the first `]`. Breaks on:
   - `jax-gpu = ["jax[cuda12]>=0.4.20"]` — captures only `"jax[cuda12`, leaving `jax-gpu` group empty in `optional_groups`
   - `all = [..., # ... `pip install lumenairy[all]`..., "refractiveindex>=1.0"]` — comment's bracket truncates capture before `refractiveindex`
   
   **The 11th walker — the v4.16.2 headline doc-surface check — silently mis-parses its canonical source.** Currently the test passes because `refractiveindex` is also picked up via the `[glass]` group, but if a future maintainer removes the dedicated `[glass]` group keeping the dep only in `[all]`, drift check goes green WITH drift. F1 phrases it as *"exactly the 'structural test bypasses production' pattern v4.16.2 was meant to retire."* Fix: use `tomllib` (Python 3.11+).

3. **🔴 P2-NEW-F2-MED-1: V11 walker hardcodes the breaking-change version list `(4.13.0, 4.15.1, 4.16.1, 4.16.2)`.** At `test_v4_16_2_dispatcher_pin_doc_consistency.py:350`. The next time a breaking change ships in (say) v4.17.0, the CHANGELOG entry will document the break, but unless someone manually edits V11 to add `'4.17.0'`, the walker passes silently. F2 phrases this as *"the exact meta-pattern V11 was supposed to close — surfaces drift, walker doesn't catch the drift."* Fix: drive the version list from a CHANGELOG scan extracting `## [X.Y.Z]` headings.

4. **🔴 P2-NEW-F1-3: `propagate_ensemble`'s `get_default_real_dtype` fallback is STRUCTURALLY UNREACHABLE DEAD CODE.** At `ensemble.py:347-355`, by the time the `except (TypeError, ValueError)` branch could fire, the earlier check at `:308-330` has already guaranteed `ensemble.dtype` exists, so `np.dtype(in_dtype).type(0).real.dtype` succeeds for EVERY numeric dtype. **The single "representative wiring" cited in the CHANGELOG and Migration-Guide is effectively dead code.** F1's verdict: *"The claim 'v4.16.2 ships representative wirings that honour this default' is mostly aspirational."*

5. **🔴 P2-NEW-F1-4: `set_default_wave_propagator` and `set_default_dy` have ZERO downstream consumers in `lumenairy/`.** Confirmed by F1 (grep) and F2 (independent grep). Repo-wide search finds NO consumers; the setters store values no library code reads. Combined with the dead-code finding above, **2 of 3 default-config knobs are no-ops in v4.16.2**. The `_resolve_default_dy` / `_resolve_default_wave_propagator` resolver functions referenced in `propagation.py:236-244` docstring **do not exist**.

**Verdict:** **All Agent A/B/C/D closures are real and well-engineered**, including the production-path integration test that closes my v4.16.1 finding. The 11th walker's design is sound; its surface scans empirically fire on synthetic drift; the Migration-Guide.md is substantive. **But three structural concerns persist:** (a) v4.16.2's flagship 11th walker contains exactly the "structural test bypasses production" pattern it was designed to retire — its regex silently mis-parses pyproject AND its version-list is hardcoded; (b) 2 of 3 default-config knobs are no-ops with no consumers; (c) the Migration-Guide example for the v4.16.2 knobs uses the wave-propagator knob that has zero consumers — a non-functional recipe. **F2's framing**: *"V11 swatted yesterday's flies and called itself a fly-killer."*

**Recommended v4.16.3 patch (~80 LOC, ~3 hours)** would close the 2 P1s via (a) `tomllib`-based pyproject parser for the 11th walker, (b) Migration-Guide.md correction or hold the recipe until v5.0 lands the resolver, plus the 6 P2 cleanups.

---

## Part 1 — Verification of v4.16.1 audit closures

### Agent A — JAX gate + ensemble dispatch (V1: ALL VERIFIED ✅)

**18/18 tests pass in 2.61s.**

| Closure | Site | Status |
|---|---|---|
| `_transfer_jax` per-call duck-typed gate | `jax_trace.py:582-606` (`hasattr(direction_n, '__array__')` + inner `np.asarray` probe catches `TracerArrayConversionError`) | ✅ |
| **`_transfer_jax` eager-only one-shot probe HOISTED to `trace_jax` entry BEFORE jit wrapper** | `jax_trace.py:1156-1174` — reads thickness via `thicks_py = jp.aux[7]` (concrete Python floats, no tracer surface) | ✅ **closes my v4.16.1 finding** |
| **End-to-end production-path integration test** | `test_v4_16_2_agent_a.py:158-204` — `test_trace_jax_end_to_end_high_na_warns` clears `_TRACE_JAX_CACHE` for cold-build, builds state via canonical `make_jax_ray_state(N=0.5*np.ones(K))`, calls `trace_jax(...)`, asserts RuntimeWarning + `'paraxial'` + `'NA'` content | ✅ **non-tautological, exercises factory path** |
| `propagate_ensemble` backend-preserving dispatch | `ensemble.py:30` (`from ..backend import array_namespace`), `:296` (`xp = array_namespace(ensemble)`), `:361` (`I_acc = xp.zeros(...)`), `:398` (`xp.abs(E_out_k)**2` — **no NumPy fallback**), `:411` (`xp.stack` for JAX) | ✅ structurally enforced |
| Empty 3-D ensemble → ValueError | `ensemble.py:322-326` | ✅ |
| `dx`/`wavelength` kwargs collision → ValueError | `ensemble.py:265-278` (`set(propagator_kwargs) & {'dx', 'wavelength'}`) | ✅ |
| `jax.grad` pin for B.4 dtype probe | `test_v4_16_2_agent_a.py:597-699` — 2 tests using `jax.value_and_grad` | ✅ |
| `propagator_kwargs` precedence pin | `ensemble.py:329-331` (`kw.update(propagator_kwargs)`); test pins **propagator_kwargs wins** | ✅ |

### Agent B — optimize/core.py (V2: ALL 8 VERIFIED ✅)

**16/16 tests pass; v4.16.0 Agent C suite clean under `-W error::UserWarning`; full sweep clean under `-W error::FutureWarning`.**

| Closure | Site | Status |
|---|---|---|
| MultiWavelengthMerit one-cycle FutureWarning | `optimize/core.py:2338` module-level latch; `:2550` guard; `:2551` global assignment; message cites v4.16.1, SUM→AVG, filterwarnings hint | ✅ |
| Constraint.__post_init__ probe → opt-in .validate() | `:3206-3246` __post_init__ no longer probes; `:3248-3255` explicit-removal comment; `:3257-3305` opt-in `.validate()` preserves shape contract | ✅ |
| Lambda warning → pickle.dumps probe | `:3230-3246` — empirically verified catches lambdas + closures + functools.partial(lambda) | ✅ |
| 5 test sites migrated to module-level fns | `test_v4_16_0_agent_c.py:188, 218, 242, 261, 545` use `_sum_constraint`/`_first_coord` | ✅ |
| `_resolve_bound` 3-tuple guard | `:4267-4281` raises on `len(b) != 2` | ✅ |
| Constraint.fun docstring example: lambda → def | `:3187-3197` | ✅ |
| LM bounds `lm`→`trf` override UserWarning | `:4287-4306` | ✅ |
| Bug 4 test name correction | v4.16.1 misnamed test now emits visible UserWarning | ✅ |

### Agent C — pre-v5.0 features (V3: ALL VERIFIED ✅)

**24/24 tests pass in 0.07s.**

**Formula-3 evaluator math hand-verified bit-exact**: V3 computed N-BK7 polynomial-form fit at λ=587.56 nm by hand:

| Term | Hand value |
|---|---|
| A0 | 2.2717635400 |
| A1·λ² | −0.0034483115 |
| A2·λ⁻² | 0.0310087564 |
| A3·λ⁻⁴ | 0.0018163633 |
| A4·λ⁻⁶ | −0.0001781379 |
| A5·λ⁻⁸ | 0.0000316530 |
| **n²** | **2.3009938632** |
| **n** | **1.5169027204** |

Library returns `1.5169027204` — **|hand − library| = 0.00e+00 (bit-exact)**. Polynomial-fit residual vs published n_d=1.5168 is 1.03e-4 (expected for 6-term polynomial; flag for v5.0 ingestion: the 5e-5 budget may need relaxation for BK7-like glasses).

All 6 setter/getter functions exist + validated + top-level exported. `POLYNOMIAL_COEFFICIENTS = {}` empty at ship (population staged for v5.0 as documented). GLASS_VALIDITY accepts numpy scalars via `(int, float, np.integer, np.floating)` at `glass.py:843`.

### Agent D — doc-surface + 11th walker (V4: ALL 8 VERIFIED ✅)

**13 + 7 = 20/20 tests pass.** README + requirements.txt + ROADMAP all synced; CHANGELOG arithmetic refreshed (2113 + 85 = 2198 ✓; A+B+C+D = 11+26+26+22 = 85 ✓); `UserWarning`→`RuntimeWarning` typo fix; `:2974`→`:3015` line citation refresh verified against live source.

**11th meta-pin walker EMPIRICALLY VALIDATED** — V4 built synthetic-drift mutations of all 6 doc surfaces:

| Synthetic drift | Walker fires? |
|---|---|
| Inject `` - `refractiveindex` `` into README Required block | ✅ YES |
| Append `pip install numpy refractiveindex` to README | ✅ YES |
| Append uncommented `refractiveindex>=1.0` to requirements.txt | ✅ YES |
| Change `ALL 11` → `ALL 8` in ROADMAP | ✅ YES |
| Change CHANGELOG headline to broken `2208/+102/2106` | ✅ YES |
| Delete Migration-Guide.md | ✅ YES |

**6/6 OK — walker is structurally sound, not a vacuous smoke test.**

**10th walker hardening structurally verified**: walks ONLY `tree.body`; explicitly rejects `FunctionDef/AsyncFunctionDef/ClassDef` nested calls (`:317-320`); explicitly rejects always-False `if False/0/None` via `ast.Constant` check (`:326-332`) while still descending into `orelse` (correct: code in else IS reachable); accepts top-level `try/except` body (canonical enrollment idiom) at `:338-345`.

**Migration-Guide.md content quality** — V4 verdict: *"This is a working migration guide, not a placeholder."* 203 lines, 5 version-specific sections + forward v5.0 section. V4 highlights the MultiWavelengthMerit section as "the strongest" with both "preserve old answer" (`weight=3.0`) AND "re-tune" recipes plus cross-config-portability argument. Citations to Wolf formula and Goodman.

---

## Part 2 — NEW findings (P0/P1/P2/P3)

### P1 findings (2)

**P1-NEW-F1-1 — Migration-Guide.md documents UNIMPLEMENTED behavior** (F1)
- Files: `Migration-Guide.md:163-175` vs `lumenairy/elements/_lens_real.py:209`
- Recipe:
  ```python
  la.set_default_wave_propagator('fresnel')
  field = la.apply_real_lens(field, prescription=pres, wavelength=wl, dx=dx)
  ```
- `apply_real_lens` hardcodes `wave_propagator: str = 'asm'` — does NOT consult `get_default_wave_propagator()`
- User copy-pasting silently uses ASM instead of Fresnel — **silent numerical-answer change**
- Lines 177-182 of Migration-Guide DO walk this back, but the misleading code example comes first
- **Fix**: either remove the recipe until v5.0 lands the resolver, OR replace with a recipe that uses a knob that actually has consumers (only `get_default_real_dtype` has a consumer — but see P2-NEW-F1-3 below)

**P1-NEW-F1-2 — 11th walker's pyproject regex silently mis-parses nested brackets** (F1)
- File: `tests/unit/test_v4_16_2_dispatcher_pin_doc_consistency.py:75-77`
- Regex: `r'^([a-zA-Z_][a-zA-Z0-9_-]*)\s*=\s*\[(.*?)\]'`
- Non-greedy `.*?` stops at first `]` — breaks on:
  - `jax-gpu = ["jax[cuda12]>=0.4.20"]` → captures only `"jax[cuda12`
  - `all = [..., # ... `pip install lumenairy[all]`..., "refractiveindex>=1.0"]` → comment's `lumenairy[all]` truncates before `refractiveindex`
- Currently still passes because `refractiveindex` is captured via `[glass]` group separately
- **But if a future maintainer removes the dedicated `[glass]` group keeping the dep only in `[all]`, drift check goes green WITH drift**
- F1's verdict: *"This is exactly the 'structural test bypasses production' pattern the audit was meant to retire."*
- **Fix**: use `tomllib` (Python 3.11+) or `tomli` instead of regex

### P2 findings (6)

**P2-NEW-F1-3 — `get_default_real_dtype` fallback is structurally UNREACHABLE dead code** (F1)
- File: `lumenairy/propagators/ensemble.py:347-355`
- The `except (TypeError, ValueError)` branch is the ONLY place `set_default_real_dtype` is consulted library-wide
- By the time control reaches line 347, the earlier check at `:308-330` has already guaranteed `ensemble.dtype` exists
- `np.dtype(in_dtype).type(0).real.dtype` succeeds for EVERY numeric dtype (int/float/complex/bool)
- The `except` branch is effectively dead code
- F1's verdict: *"The claim 'v4.16.2 ships representative wirings that honour this default' is mostly aspirational."*
- A user calling `set_default_real_dtype(np.float32)` and passing a complex128 ensemble still gets a float64 accumulator
- **Fix**: either (a) move the `get_default_real_dtype()` call BEFORE the try/except so it's the canonical fallback path, OR (b) honestly document the knob as "API-only in v4.16.2; consumer wiring follows in v5.0"

**P2-NEW-F1-4 — `set_default_wave_propagator` + `set_default_dy` have ZERO downstream consumers** (F1 + F2 2-way confirm)
- Files: `lumenairy/propagators/propagation.py:287-350`
- Repo-wide search for `get_default_wave_propagator|get_default_dy|DEFAULT_WAVE_PROPAGATOR|DEFAULT_DY` finds NO consumers in `lumenairy/`
- Setters store values no library code reads
- Combined with P2-NEW-F1-3: **2 of 3 default-config knobs are no-ops in v4.16.2**
- The `_resolve_default_dy` / `_resolve_default_wave_propagator` resolver functions referenced in `propagation.py:236-244` docstring **do not exist**
- **Fix**: either (a) ship at least one consumer for each knob (e.g., propagator dispatch in `propagate`), OR (b) emit a `UserWarning` from the setters that the knob is "API-only in v4.16.2; takes effect in v5.0"

**P2-NEW-F2-MED-1 — V11 walker hardcodes breaking-change version list** (F2)
- File: `test_v4_16_2_dispatcher_pin_doc_consistency.py:350`
- `test_migration_guide_has_known_version_sections` iterates over hardcoded tuple `('4.13.0', '4.15.1', '4.16.1', '4.16.2')`
- v4.17.0 with breaking change: CHANGELOG documents the break, but unless someone manually edits V11 to add `'4.17.0'`, walker passes silently
- **F2's verdict**: *"This is the exact meta-pattern V11 was supposed to close — surfaces drift, walker doesn't catch the drift."*
- **Fix**: drive version list from CHANGELOG scan extracting `## [X.Y.Z]` headings; cross-reference with sections flagged "Breaking" / "silent semantics change" / "DeprecationWarning"

**P2-NEW-F2-MED-2 — CHANGELOG ↔ Migration-Guide drift uncovered** (F2)
- V11 only asserts Migration-Guide.md exists and contains 4 hardcoded version headings
- Does NOT scan CHANGELOG for entries flagged as "breaking" or "silent semantics change" and verify each has a corresponding Migration-Guide section
- CHANGELOG line 1403 says "v4.15.1 ships 3 confirmed breaking items" — V11 doesn't enumerate them or verify their migration recipes exist
- **Fix**: extend V11 to scan CHANGELOG for breaking-change tags and assert Migration-Guide coverage

**P2-NEW-F1-1 — Constraint auto-probe removal has no DeprecationWarning** (F1)
- File: `lumenairy/optimize/core.py:3214-3242`
- v4.16.1 shipped auto-probe; v4.16.2 silently removed
- Users who came to rely on v4.16.1 auto-probe (1 release of exposure) now silently push vector-returning `fun`s through optimizer and hit old runtime `TypeError`
- Compare to v4.16.1 SUM→AVG transition which DID get FutureWarning in v4.16.2 — **same silent-behavior-shift treated inconsistently**
- **Fix**: emit one-cycle DeprecationWarning when `Constraint(...)` is constructed without prior `.validate()` call, scheduled for removal in v5.0

**P2-NEW-F1-2 — `pickle.dumps` probe catch list too narrow** (F1 + V2 2-way confirm)
- File: `lumenairy/optimize/core.py:3229-3232`
- `except (pickle.PicklingError, AttributeError, TypeError) as e:` misses:
  - `RecursionError` (deep object graph)
  - `RuntimeError` (custom `__reduce__`)
  - `MemoryError` (huge object)
  - Arbitrary exceptions from `__reduce__` / `__getstate__`
- A `fun` whose `__reduce__` raises `RuntimeError` propagates out of `Constraint(...)` construction, **crashing instantiation** — defeats the warning's "friendlier than rejecting" intent
- **Fix**: `except Exception` — pickling is best-effort heuristic; any failure is "not safely picklable" signal

### P3 findings (8)

- **P3-NEW-F1-1**: `POLYNOMIAL_COEFFICIENTS` dispatch is fallback-only. When `refractiveindex` IS available (the standard `pip install lumenairy[glass]` recommendation), the bundled polynomial evaluator is unreachable. No `__polynomial__` sentinel parallel to `__sellmeier__`. v5.0 ingestion will need explicit sentinel.
- **P3-NEW-F1-2**: One-shot latches `_MULTIWL_AVG_WARNED` + `_TRANSFER_JAX_WARNING_EMITTED` not fork-safe — under `differential_evolution(workers>1)` with `spawn` each worker re-imports module and re-emits once; with N=8 workers user sees 8 warnings. Not documented.
- **P3-NEW-F1-3**: Default-config knob module-level globals not pickle/fork-safe — child workers under spawn-mode re-import and reset to defaults, silently overriding parent's `set_default_*` calls.
- **P3-NEW-F1-4**: Conservative high-NA hoist uses `max(thickness)` not per-surface — overstates worst-case drift in the user-facing message.
- **P3-NEW-F1-5**: `psutil>=5.0` listed under "Recommended" in requirements.txt but is hard dep in pyproject.toml (V11 only flags optional→required, not vice versa).
- **P3-NEW-F2-LOW-1**: `DEFAULT_REAL_DTYPE`/`DEFAULT_WAVE_PROPAGATOR`/`DEFAULT_DY` NOT re-exported at top level; `DEFAULT_COMPLEX_DTYPE` IS — sibling-gap in module-level constants. V9 doesn't catch because `propagation.py` has no `__all__`.
- **P3-NEW-F2-LOW-2**: "structurally retired" claim at CHANGELOG:148-149 and ROADMAP:85-87 is overreaching. Honest statement: "structurally retired at known classes; new classes will continue to surface."
- **P3-NEW-V3-1**: CHANGELOG/audit-prompt says dispatch order "POLYNOMIAL → SELLMEIER"; actual code at `glass.py:1031-1051` does "SELLMEIER → POLYNOMIAL". Benign because lookup keys don't overlap and `POLYNOMIAL_COEFFICIENTS` is empty, but doc/code drift.

---

## Part 3 — Cross-cutting analysis

### All 11 dispatcher meta-pins active and green ✅

| Pin | Status | Notes |
|---|---|---|
| V1 cache-clears | GREEN | |
| V2 cache↔lock | GREEN | 2 documented skip exemptions |
| V3 0+0j | GREEN | |
| V4 validate_grid_params | GREEN | 1 documented xfail |
| V5 check_2d_scalar_field | GREEN | |
| V6 sentinel-propagation | GREEN | |
| V7 xp-dispatch | GREEN | |
| V8 dy-threading | GREEN | |
| V9 __all__-symmetry | GREEN | |
| V10 cache-registry | GREEN | HARDENED in v4.16.2 |
| **V11 doc-consistency** | **GREEN** | **NEW; empirically validated on 6 synthetic-drift mutations; BUT contains 2 structural issues (P1-NEW-F1-2 + P2-NEW-F2-MED-1)** |

**Empirical (F2 ran):** 262 passed, 2 skipped, 1 xfailed across all 11 dispatcher pins.

### Meta-pattern recurrence trajectory

| Round | Closure scope | Sibling-gap that emerged |
|---|---|---|
| v4.15.2 | 10 propagator guards | 9 siblings in same dirs |
| v4.15.3 | walker scoped to 2 packages | 4 entry points in 2 other packages |
| v4.15.4 | `__all__`-walker + 4 packages | 30+ outside name-filter |
| v4.15.5 | V6 first-positional-param-name | (closed at func surface) |
| v4.16.0 | 9 walkers across all known classes | 3 NEW classes (GLASS_VALIDITY, @lru_cache, optional-dep) |
| v4.16.1 | V10 closes @lru_cache; GLASS_VALIDITY consistency | 4 doc-surface drifts (README/requirements/ROADMAP/CHANGELOG arithmetic) |
| **v4.16.2** | **V11 closes doc-surface drifts** | **V11's own regex silently mis-parses; V11's version list hardcoded; 2/3 default-config knobs no-ops** |

**F2's verdict (verbatim):** *"V11 swatted yesterday's flies and called itself a fly-killer. The meta-pattern has NOT actually been retired."* Each generation of walker closes the previous surface; each release introduces a new surface (or new structural bypass in the walker itself).

### Test count reconciliation — VERIFIED EMPIRICALLY

| Metric | CHANGELOG | Empirical (F2 ran) |
|---|---|---|
| Collected | 2276 | **2276** |
| Pass | 2270 | **2270** |
| Skip | 5 | **5** |
| xfail | 1 | **1** |
| Net delta vs v4.16.1 (2198) | +78 | reconciles: 2198 + 78 = 2276 ✓ |
| Per-agent breakdown | A=18 + B=16 + C=24 + D=20 (13+7) = 78 | **78 verified** ✓ |

**The arithmetic actually matches this time.** v4.16.1's 2208/+102/2106 confusion is fully closed.

### Deferred-item integrity ✅

All previously-deferred items confirmed intact at HEAD:
- `POLYNOMIAL_COEFFICIENTS = {}` empty at ship (population staged for v5.0) ✓
- Modal-asymptotic independent ground-truth pin: deferred
- MCF-aware downstream propagators: deferred (refreshed message in v4.16.1)
- `MultiPrescriptionParameterization.scale_floor`: deferred (accessed via `getattr(..., 'scale_floor', None)` at `optimize/core.py:4050`, no class attribute)
- Forbes Q-2D-asymmetric: deferred
- God-module splits: deferred
- 14 modules + `__all__` migration: PARTIAL (`propagation.py` still lacks `__all__`)
- Tier-1/tier-2 namespace split: deferred
- README rewrite: DONE — `git diff --stat`: README.md +163 lines

### Stray-file cleanup ✅

`git status` clean; no untracked files; dotfile release notes (`.release_notes_v4_16_2_agent_{a,b,c,d}.md`) correctly placed in `docs/release_notes/`.

### Convergences (multi-agent independent confirmations)

| Finding | Streams | Severity |
|---|---|---|
| Default-config knobs have zero/unreachable consumers | F1 + F2 (2-way) | P2 |
| `pickle.dumps` catch list too narrow | F1 + V2 (2-way) | P2 |
| Migration-Guide §4.16.2 recipe uses non-functional knob | F1 + F2 (2-way) | P1+P2 cluster |
| "structurally retired" claim overreaching | F2 only, but consistent with F1's V11 regex finding | P3 |

---

## Part 4 — Tier-0 v4.16.3 patch recommendations

**P1 closures (2):**

1. **Fix 11th walker pyproject parsing**: replace regex with `tomllib` (Python 3.11+; library declares `requires-python = ">=3.9"` but `tomli` is the 3.9-compatible backport). Verify the resulting walker correctly handles `jax-gpu = ["jax[cuda12]>=0.4.20"]` and `all = [..., # `lumenairy[all]`..., ...]`.

2. **Migration-Guide.md §4.16.2 correction**: either (a) remove the `set_default_wave_propagator` recipe until v5.0 lands the consumer wiring, OR (b) replace with a recipe that uses `set_default_real_dtype` (the one knob with at least an attempted consumer) and document the limitation. Adding a "v4.16.2 limitation note" inline is acceptable.

**P2 closures (6):**

3. **Fix `get_default_real_dtype` consumer wiring**: move the call BEFORE the try/except in `ensemble.py:347-355` so it's the canonical fallback path, not unreachable dead code. Alternative: honestly document the knob as "API-only in v4.16.2; consumer wiring follows in v5.0".

4. **Ship a real consumer for at least one of `set_default_wave_propagator` / `set_default_dy`**, OR emit `UserWarning` from the setters that the knob is "API-only in v4.16.2; takes effect in v5.0".

5. **V11 version list → CHANGELOG-driven**: replace hardcoded tuple `('4.13.0', '4.15.1', '4.16.1', '4.16.2')` with a CHANGELOG scan extracting `## [X.Y.Z]` headings flagged with breaking-change tags.

6. **V11 extends to CHANGELOG↔Migration-Guide drift coverage**: assert every CHANGELOG entry flagged "breaking" / "silent semantics change" has a Migration-Guide section.

7. **Add DeprecationWarning for Constraint auto-probe removal** to maintain pattern consistency with the v4.16.2 MultiWavelengthMerit FutureWarning treatment.

8. **Widen `pickle.dumps` catch list** to `except Exception` — pickling is best-effort heuristic.

**P3 cluster (8):** sentinel-based POLYNOMIAL dispatch parallel to `__sellmeier__`; document/fix one-shot latch multiprocess semantics; document default-config knob fork-safety; per-surface (not max) thickness in high-NA hoist message; `psutil` requirements.txt promotion; re-export `DEFAULT_*` constants at top level for sibling parity with `DEFAULT_COMPLEX_DTYPE`; soften "structurally retired" claim in CHANGELOG + ROADMAP; reconcile POLYNOMIAL/SELLMEIER dispatch order between code and docs.

**Aggregate**: ~80 LOC + 5-7 regression tests; ~3 hours of focused work.

---

## Part 5 — Yield trajectory + audit-process meta-findings

### Yield

| Release | P0 | P1 |
|---|---|---|
| v4.15.5 | 0 | 0 |
| v4.16.0 | 0 | 4 |
| v4.16.0 deep | 0 | 14 |
| v4.16.1 | 0 | 5 |
| **v4.16.2** | **0** | **2** |

**v4.16.2 has the lowest P1 yield since v4.15.5.** Zero physics regressions, zero math errors. The 2 P1s are both in the doc/UX surfaces (Migration-Guide recipe + V11 regex), not in the substantive code closure work. **The deep-audit recurrence pattern continues but at successively-narrowing scopes.**

### Wrong-reason rate

- 22 Agent A/B/C/D closures verified by V1-V4 with non-tautological pins
- Formula-3 math hand-verified bit-exact against Schott old-polynomial fit
- 11th walker empirically validated on 6 synthetic doc-surface mutations (6/6 OK)
- Production-path integration test for `_transfer_jax` warning now exercises the canonical `make_jax_ray_state` factory pipeline (closing the v4.16.1 gap)
- F1 + F2 surfaced 2 P1 + 6 P2 the verifier audits couldn't see — particularly the structural-bypass issues inside V11 itself

### What v4.16.2 got STRONGLY right

1. **Production-path integration test for `_transfer_jax`** — clears `_TRACE_JAX_CACHE` for cold-build, builds state via canonical factory, calls `trace_jax(...)`, asserts warning content. Exactly the gap V2 of v4.16.1 missed.
2. **Backend-preserving `propagate_ensemble` rewrite** structurally enforced — `xp.zeros / xp.abs / xp.stack` with no NumPy fallback in per-realization loop.
3. **Formula-3 evaluator math is bit-exact** against hand-derivation (V3 verified to floating-point precision).
4. **10th walker hardening** correctly rejects function-nested + `if False:` enrollment calls.
5. **11th walker's 6 surface scans empirically fire** on synthetic drift (validated by V4).
6. **Migration-Guide.md is substantive** — working recipes per breaking change, not a stub.
7. **Test count arithmetic finally reconciles cleanly**: 2113 + 85 + 78 = 2276 collected; A+B+C+D = 18+16+24+20 = 78 ✓.
8. **CHANGELOG corrections landed** for v4.16.1's `UserWarning`→`RuntimeWarning` typo, `2208/+102/2106` arithmetic, `:2974`→`:3015` line citation.
9. **One-cycle FutureWarning latch** for MultiWavelengthMerit SUM→AVG transition closes my v4.16.1 silent-behavior-change concern.
10. **Constraint pickle.dumps probe** empirically catches lambdas + closures + `functools.partial(lambda)`.

### What v4.16.2 needs to improve

1. **V11 walker's own structural integrity** — its regex silently mis-parses pyproject AND its version list is hardcoded. The walker whose job is "catch documentation-surface drift" contains exactly the "structural test bypasses production" pattern.
2. **Default-config knob consumer wiring** — 2 of 3 knobs are no-ops; the 1 wired knob has unreachable dead-code consumer. Either ship consumers or honestly document as "API-only in v4.16.2".
3. **Migration-Guide §4.16.2 recipe accuracy** — the documented recipe demonstrates a knob with zero consumers, silently producing the wrong propagator.
4. **Treatment consistency for silent behavior changes** — v4.16.2 added FutureWarning for MultiWavelengthMerit but no analogue for Constraint auto-probe removal.
5. **"Structurally retired" overreach** in CHANGELOG + ROADMAP. Honest framing is "retired at known classes; new classes will continue to surface."

---

## Final verdict

**v4.16.2 is a substantively solid closure release with one persistent structural concern.** All 22 v4.16.1 audit items close cleanly. The production-path integration test that closes my v4.16.1 finding is present and non-tautological. The backend-preserving `propagate_ensemble` rewrite is structurally enforced. The formula-3 evaluator math is bit-exact. The Migration-Guide.md is substantive. All 11 dispatcher meta-pins green. Test count arithmetic reconciles exactly.

**However, the meta-pattern continues to recur — this time inside the very walker (V11) designed to retire it.** V11 contains:
- A silent regex mis-parsing bug that would mask future doc drift (P1-NEW-F1-2)
- A hardcoded version list that introduces new sibling-gap (P2-NEW-F2-MED-1)
- An empty CHANGELOG↔Migration-Guide drift coverage (P2-NEW-F2-MED-2)
- A documented recipe in Migration-Guide.md that demonstrates a knob with zero consumers (P1-NEW-F1-1)

Plus 2 of 3 default-config knobs have zero downstream consumers and the 1 wired knob has unreachable dead-code consumer.

**The pattern hasn't been retired — it has miniaturized again.** The good news: the gap-classes are progressively narrower each round (function-level → package-level → __all__-coverage → name-filter → class-method → optional-dep → cache-registry → doc-surface → V11's own internal consistency). Each release does close the previous surface; each release surfaces new surfaces that the prior generation of walkers couldn't see.

**Recommended**: v4.16.3 patch (~80 LOC, ~3 hours) closing the 2 P1s + 6 P2s. After items #1-4 above (`tomllib`-based V11; Migration-Guide §4.16.2 correction; real consumer wiring for default-config knobs; DeprecationWarning for Constraint auto-probe removal), v4.16.x reaches a defensible "ready to publish" state. **The architectural cleanup remaining is v5.0 scope per the established ROADMAP.**

The fundamental engineering remains exceptional. The deep-audit + verification-audit pattern is itself working as designed: each generation surfaces new classes the prior generation's walkers couldn't catch; the structural counter-measures progressively tighten; the remaining yield is in successively narrower surfaces. **v4.16.2 has the lowest P1 yield since v4.15.5 (2 vs 0); zero physics regressions in 4 consecutive releases.**
