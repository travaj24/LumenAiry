# Lumenairy v5.0.1 — Audit Report

**Date:** 2026-05-20
**Library state:** v5.0.1 tag at `964b753` + 7 CI follow-up commits through HEAD `90fd2e8` (de-facto v5.0.2 state).
**Scope:** (a) verify v5.0.1 closes the v5.0.0 audit (3 P1 + 5 P2 + 8 P3 = 16 items); (b) audit the 7 post-tag CI follow-up commits; (c) cross-cutting + meta-pattern recurrence + retrospective on the audit cadence.

**Methodology:** 6 parallel single-domain auditors (V1, V2, V3, V4, F1, F2). v5.0.1 + 7 CI follow-ups: +2471 LOC / -219 LOC across 26 files; the library-internal touch is small (TYPE_CHECKING guards, distortion-grid FP boundary widening, warning text v5.0→v5.1) — most of the diff is CI workflow hardening, tests, and docs.

---

## Part 0 — TL;DR

**v5.0.1 closes its declared audit scope cleanly** (V1: 3/3 P1 verified; V2: 4/5 P2 verified + 1 PARTIAL; V3: 9/9 P3 verified). **All 11 dispatcher meta-pins green** (V1-V11; 259 passed + 2 documented skips + 1 documented xfail in 4.81s). Library-internal code work is quality (TYPE_CHECKING guards are textbook-correct, not paper-overs; distortion-grid FP boundary fix is physically justified ~4500 ULP widening; benchmark double-break is mathematically correct doubling; warning text + pinning tests genuinely aligned). **`__all__` arithmetic now reconciles**: 533 entries via 537 smoke tests (533 parametrized + 4 standalone — the v5.0.1 counter-pin added a 4th).

**HOWEVER, three independent agents (V4 + F1 + F2) converged on an unambiguous P1 process finding the v5.0.0 audit MISSED:** v5.0.1 was tagged before CI was ever observed green. The 7 CI follow-up commits in <3 hours after the v5.0.1 tag are the evidentiary record:

| Time | Commit | What surfaced |
|---|---|---|
| 15:08 | `964b753` v5.0.1 tag | Claims "2889 unit tests pass + lint advisory + CI gates wired" |
| 17:11 (+2h 03m) | `abf2294` | **Workflow installed `[all]` extras; pytest lives in `[dev]`. Every unit-test job since v5.0.0 ship had failed at `python -m pytest`** |
| 17:21 (+10m) | `50454e7` | Drops Python 3.10 from matrix (creates floor-bound sibling-gap with `requires-python = ">=3.10"`) |
| 17:27-17:35 | `a876309`, `6591344`, `4171eec` | 3 layers of diagnostic plumbing because CI logs were inaccessible |
| 17:45 (+10m) | `069ad64` | Library + test fix: 4 PySide6 failures + 1 distortion-grid FP boundary |
| 17:56 (+11m) | `1d97e57` | 5 more PySide6 failures (6 TestUI classes need `@_skip_no_qt`) |
| 18:01 (+5m) | `90fd2e8` | 1 more failure: `test_examples_output_dir` gitignore-vs-CI mismatch |

**The v5.0.0 audit's V4 had said *"Unit job (3.10-3.13 matrix): clean, reproduces 2858/5/1 ✓"* — that verification was done LOCALLY against the test files, NOT by triggering the workflow.** The CHANGELOG's "2889 unit tests pass / 34/34 validation pass" line in v5.0.1's entry is the local-machine result. This is the same audit value-add issue F2 frames retrospectively: *"V1-V4 trust the workflow without running it."*

**F2's verdict (verbatim)**: *"HEAD is shippable as `v5.0.2`, not as `v5.0.1`. Calling HEAD 'v5.0.1' obscures the fact that 7 patches were needed to make v5.0.1's CI claims true."*

**Headline new findings (sorted by severity):**

1. **🔴 P1-NEW-3WAY-1 (V4 + F1 + F2 3-way convergent): v5.0.1 was NOT CI-tested before ship.** Workflow installed `[all]` extras (no pytest); 8 post-tag commits required to make CI green. CHANGELOG framed as "shipped clean" — misleading. The v5.0.0 audit missed this because verifier audits read the workflow but did not trigger it. `publish.yml` still has no `needs: [unit, validate]` gate — next release could ship the same way.

2. **🔴 P2-NEW-V4-G (V4): TestUI3 + TestUI4 have `@_skip_no_qt` doubled** at `test_v4_15_agent_e.py:215-216, 254-255`. Functionally harmless (skipif decorators stack idempotently) but a sloppy diff applied without re-reading the file.

3. **🔴 P2-NEW-V4-E (V4): FAILED test annotations use bare `::error::` not `::error file=...::`.** Plus the greedy grep pattern `"^FAILED |^ERROR |error |Error:"` matches noise like `RuntimeError:` and `error in pip`. Functional but coarse.

4. **🔴 P2-NEW-3WAY-2 (V4 + F1 + F2 3-way convergent): CI matrix `['3.11', '3.12', '3.13']` doesn't include 3.10 despite `requires-python = ">=3.10"`.** Documented gap in `unit-tests.yml:23-26` comment. The `tomli` fallback path in V11 walker (`test_v4_16_2_dispatcher_pin_doc_consistency.py:51-78`) is now CI-untested. A 3.10-specific regression (e.g., `match` statement quirks, `Self` type, `from __future__ import annotations` patterns) won't be caught.

5. **🔴 P2-NEW-V2: 3 of 5 shim-removal pins lack `match=` migration-recipe text**. Pins for `lumenairy.ao`, `lumenairy.io.hdf5`, `lumenairy.system` top-level are bare-raises against `ImportError/ModuleNotFoundError` (following original v5.0 `test_analysis_dot_analysis_shim_removed_in_v5_0` template). The audit brief required all 5 to use `pytest.raises(..., match=...)` locking in migration-recipe text. The 2 kwarg/schema pins (JAX aperture, `cosmic_ray_rate`) DO use `match=` correctly.

6. **🔴 P2-NEW-F1-3: `test_examples_output_dir` source-inspection check too loose**. After `90fd2e8` refactor: `assert "examples/output" in src or "'output'" in src or '"output"' in src`. Second/third disjuncts match ANY 'output' substring anywhere. Maintainer could refactor to write PNGs elsewhere while keeping a `print("output saved")` and pin silently passes. Intent of v4.16.1 centralization no longer enforceable.

7. **🟡 P2-NEW-F1+F2: CHANGELOG v5.0.1 entry does not acknowledge the 8 post-tag CI fix commits.** Future maintainer reading the CHANGELOG will believe v5.0.1 shipped clean. Honest framing: re-tag as v5.0.2 with the 8 fixes documented OR add a `[5.0.1.post1]` block.

**Verdict**: v5.0.1 is the cleanest release in feature-correctness terms since v4.15.5 (V2/V3/F1 all reported "zero physics regressions, zero code-correctness bugs in v5.0.1 diff itself"). **But the release process discipline broke down.** F2's framing of HEAD as de-facto `v5.0.2` is correct: HEAD is the first commit where the CI workflow successfully runs.

---

## Part 1 — Verification of v5.0.0 audit closures

### V1: P1 closures (3 items) — ALL VERIFIED ✅

`tests/unit/test_v5_0_1_agent_a.py`: **4/4 pass in 2.29s**.

| Closure | Site | Status |
|---|---|---|
| `set_default_*` UserWarning v5.0→v5.1 | `propagation.py:382, :435` + pinning test `test_v4_16_3_agent_b.py:89, :183` | ✅ — v4.16.3 "default-knob honesty" closure now genuinely honest |
| Benchmark double-break | `benchmarks/test_bench_jax_jit.py:104, :114` | ✅ — `radius=200e-6 → diameter=400e-6` mathematically equivalent (2× doubling) |
| F821 fix + advisory lint | `lumenairy/algebra/base.py:29-42`, `lumenairy/propagators/system.py:11-27`, `.github/workflows/unit-tests.yml:123` | ✅ — **REAL fix, not paper-over** |

V1 quality assessment of F821 fix: *"Textbook correct fix for forward-ref bugs in lazy-import architectures, not a `# noqa` paper-over. The string-quoted annotations are now resolvable by static analyzers and IDEs; JAX trace still works; F821 lint passes cleanly."* Decision to land lint in advisory mode (with explicit inline comment committing v5.1 cleanup) is *"coherent roadmap alignment"*.

### V2: P2 closures (5 items) — 4 VERIFIED, 1 PARTIAL

| Closure | Status |
|---|---|
| `simulate_detector_image` → `apply_detector` rename | ✅ — zero source matches; Migration-Guide.md:275 + CHANGELOG.md:61,64 + README.md:42 historical only |
| 5 shim-removal anti-regression pins | **⚠️ PARTIAL** — only 2 of 5 use `match=` (JAX aperture + cosmic_ray_rate); 3 import pins are bare-raises following v5.0 template |
| ROADMAP v5.1 section refresh | ✅ — line 3 header refreshed; line 23-97 "Current state" describes v5.0.0 baseline; lines 159-173 file-split LOC counts refreshed; lines 272-288 new "Active back-compat shims at v5.0 (intentionally kept)" subsection enumerates all 3 `apply_*_lens` re-exports |
| 2 stale docstrings | ✅ — `detector.py:82-96` cosmic_ray_rate removal note + migration recipe; `system.py:591` `la.system.evaluate → la.evaluate` |
| mypy `follow_imports = "silent"` | ✅ — empirically 63 errors in 8 files (matches V4's count exactly); 1889-error cascade silenced; CI activation deferred to v5.1 |

**PARTIAL pin coverage detail (P2-NEW-V2)**: V2's recommendation: *"the 3 import pins would need `match='moved to|use lumenairy\\..*'` style migration pointers"* — a future maintainer could re-add a hollow shim that re-raised manually and still satisfy the bare-raises contract.

### V3: P3 closures (9 items) — ALL VERIFIED ✅

`tests/unit/test_v5_0_1_agent_c.py`: **21/21 pass in 0.09s**. Per-agent reconciles exactly: A=4 + B=6 + C=21 = +31 net (2858→2889).

Notable verifications:
- **`__all__` arithmetic**: live `len(lumenairy.__all__) == 533`; CHANGELOG correctly reads "533 entries verified via 536 smoke tests" at v5.0 ship; v5.0.1 added a 4th standalone counter-pin so live count is 537 (CHANGELOG correctly phrased as v5.0-state statement, not a v5.0.1 drift).
- **MCF deferral clarification**: live `hasattr(la, 'PartialCoherenceMCF') == True` (already shipped v4.15.1 at `sources/core.py:1410`); `hasattr(la, 'MCF') == False` (v5.1 deferral is correctly the SHORT ALIAS, not the object itself).
- **`test_public_api.py` counter-pin design** (V3 highlight): pre-asserts phantom not in `__all__`; appends in `try`, asserts `hasattr(la, phantom) is False` (proves smoke isn't vacuous because `name in dir(la)` would NOT include the phantom even when in `__all__`); in `finally` asserts phantom still present (detects concurrent mutation), removes it, asserts gone. F1 separately verified: SIGINT/SIGTERM concerns are non-issues — `finally` runs on SIGINT; SIGTERM kills process so no persistent leak across runs.
- **Unreachable post-raise code removed** at `system.py:929-945` (post-edit; pre-edit was 932-935) — `_reject_legacy(...)` always raises; subsequent tuple return verifiably gone.

Minor non-failure observations:
- CHANGELOG line-number citations for closures 7+8 (`system.py:932-935` and `:1242-1248`) slightly off post-edit actual locations (`:929-945` and `:1251-1257`) due to TYPE_CHECKING block additions upstream. Both edits land at correct semantic sites; content matches.

---

## Part 2 — Verification of 7 CI follow-up commits (V4)

V4 verdict: *"Substantive, not duct tape — but the gate that mattered is still missing."*

| Commit | Status | Notes |
|---|---|---|
| A `abf2294` `[all,dev]` install | ✅ VERIFIED | **`[all]` doesn't include pytest**; every test job since v5.0 was 100% failing |
| B `50454e7` align with validate.yml + timeouts + maxfail | ✅ VERIFIED | CONCERN-light: Python 3.10 dropped from matrix despite pyproject `>=3.10` floor |
| C `a876309` artifact upload on failure | ✅ VERIFIED | `tee + exit ${PIPESTATUS[0]}` correctly propagates pytest exit code |
| D `6591344` GITHUB_STEP_SUMMARY tail | ✅ VERIFIED | `tail -n 200` bounded; sensible |
| E `4171eec` FAILED test annotations | ⚠️ PARTIAL | Uses bare `::error::` not `::error file=...::`; greedy grep matches noise |
| F `069ad64` distortion-grid FP boundary | ✅ **REAL FIX, not paper-over** | Physical reasoning: at `L²+M² = 1-1e-12`, `N = sqrt(1e-12) = 1e-6` so `t = z/N` is still unphysical by 6 orders of magnitude. ~4500 ULP widening at `|x|≈1`. `test_30_deg_grid_runs` still passes (tolerance doesn't fire on legitimate grids). |
| G `1d97e57` `@_skip_no_qt` on TestUI classes | ⚠️ PARTIAL | **TestUI3 + TestUI4 have `@_skip_no_qt` DOUBLED** at lines 215-216, 254-255 — functionally harmless (skipif stacks idempotently); sloppy diff |
| H `90fd2e8` test_examples_output_dir | ⚠️ PARTIAL | Legitimate refactor from filesystem to source-inspection, but check is too loose (F1 P2-3) |

**Critical structural finding (V4)**: **None of the 8 commits address the `publish.yml` release gate.** Release-publish job fires on GitHub Release event with zero dependence on CI green. **The next v5.0.2 could ship the same way.** Fix is one line in `publish.yml` (`needs: [unit, validate]`) plus enabling branch protection.

---

## Part 3 — Cross-cutting analysis (F1 + F2)

### All 11 dispatcher meta-pins active and green ✅

**259 passed + 2 skipped + 1 xfailed** across V1-V11 in 4.81s. The 2 skips (`_REGISTRY_LOCK`, `_ZARR_MKDIR_PATCH_LOCK`) and 1 xfail (`create_led_source` validator) are documented exemptions.

### Meta-pattern recurrence: status

**v5.0.1 closed the v5.0.0 fresh sibling-gap (asymmetric shim-removal pins). One new infrastructure-class instance found**: CI matrix lower-bound floor not pinned against `pyproject.requires-python`. The `test_v5_0_1_agent_c.py::TestPyprojectClassifiersMatchCIMatrix` only checks upper bound (3.14 absent) and canonical set present — doesn't check actual matrix in `unit-tests.yml` or assert `min(matrix) == requires-python` floor.

**F2 retrospective on meta-pattern trajectory**:

| Round | Surface | Sibling-gap that emerged |
|---|---|---|
| v4.15.2 | 10 propagator guards | 9 sibling guards in same dirs |
| v4.15.3 | walker scoped to 2 packages | 4 entry points in 2 other packages |
| v4.15.4 | `__all__`-walker + 4 packages | 30+ outside name-filter |
| v4.15.5 | V6 first-positional-param-name | (closed at function surface) |
| v4.16.0 | 9 walkers across all known classes | 3 NEW classes (GLASS_VALIDITY, @lru_cache, optional-dep) |
| v4.16.1 | V10 closes @lru_cache | 4 doc-surface drifts |
| v4.16.2 | V11 closes doc-surface drifts | V11's own internal sibling-gap (regex mis-parse, hardcoded version list) |
| v4.16.3 | V11 hardening + default-knob honesty | (clean closure; 0 P1) |
| v5.0.0 | Breaking changes + CI gates added | 4 P1s, all infrastructure |
| v5.0.1 | Audit closure | **0 P1 in declared scope; 1 P1 (CI verification process) caught by 3-way convergence; 1 P2 sibling-gap (CI matrix floor)** |

**F2's verdict (verbatim)**: *"Meta-pattern is structurally retired for code surfaces. Declaring 'meta-pattern narrowed past actionable' is defensible — future occurrences will be infrastructure-class, single-paragraph closures, not multi-cluster releases."*

### Deferred items at HEAD — ALL VERIFIED INTACT (F2 + F1)

| Item | Status at HEAD |
|---|---|
| 6 monolith file splits | All still monolithic: `propagation.py` 4095, `system.py` 1299, `sources/core.py` 2991, `analysis/field.py` 1268, `glass.py` 1217, `io/storage.py` 1592, `optimize/core.py` 4538, `analysis/core.py` 4088, `propagators/asymptotic.py` 4561 |
| Default-config knob consumers | Zero consumers (sibling-gap pin still passes) |
| `lumenairy.MCF` top-level alias | `hasattr(la, 'MCF') == False`; `PartialCoherenceMCF` is the canonical class |
| Off-axis conic | Not shipped (zero grep matches) |
| `POLYNOMIAL_COEFFICIENTS` | Still empty dict at `glass.py:340` |
| 5 new examples | Not shipped (`examples/` has 11 .py files; ROADMAP's 5 specific are absent) |
| 57 `test_audit_fixes_*` consolidation | Still 57 files |

### Test count reconciliation — VERIFIED EMPIRICALLY

| Metric | CHANGELOG | F2 empirical |
|---|---|---|
| Collected | 2895 | **2895** ✓ |
| Pass | 2889 | **2888** (1 timing flake: `test_warm_call_at_least_200x_faster_than_first` — passes in isolation; not regression) |
| Skip | 5 | **5** ✓ |
| xfail | 1 | **1** ✓ |
| Net delta vs v5.0.0 (2858) | +31 | reconciles (A=4 + B=6 + C=21 = 31) |

### Items verified clean (extensive, F1)

- **TYPE_CHECKING fix**: `from __future__ import annotations` defers all annotations to string form; `if TYPE_CHECKING:` provides typing-time-only binding; runtime imports stay lazy. Zero grep hits for `get_type_hints` or `__annotations__` introspection in `lumenairy/`. Eager + JAX trace both verified unaffected.
- **All 5 shim-removal pins assert removal correctly** (with V2's match= caveat for 3 of them)
- **NO silent shipments** across all v5.1+ deferred items
- **mypy `follow_imports="silent"` does NOT mask scope-local errors**: empirically 63 errors in 8 files (exact match to V2's count and to advertised number)
- **F821 regressions caught by `test_v5_0_1_agent_a.py::test_no_f821_forward_reference_bugs_in_lumenairy`** which runs `ruff --select F821` as non-advisory unit-test subprocess. Other lint codes are advisory-only — but those are not code bugs.
- **`_ZERO_APERTURE_MASK` citation at `:3032` correct** at v5.0.1
- **Counter-pin SIGINT/SIGTERM concerns are non-issues** — `finally` runs on SIGINT; SIGTERM kills process so no cross-run leak

---

## Part 4 — NEW findings (P0/P1/P2/P3)

### P1 findings (1, 3-way convergent)

**P1-NEW-3WAY-1 (V4 + F1 + F2)**: **v5.0.1 was NOT CI-tested before ship.** Detailed evidence in TL;DR table above. The v5.0.0 audit missed this because verifier audits (V1-V4) read the workflow YAML but did not actually trigger it. CHANGELOG framed v5.0.1 as "shipped clean with 2889 unit pass / 34/34 validation pass" — that's the local-machine result. CI was never green at the v5.0.1 tag.

- **Process-P2 sibling**: `publish.yml` has no `needs: [unit, validate]` gate. The next v5.0.2 could ship the same way unless this is addressed.

### P2 findings (5)

**P2-NEW-3WAY-2 (V4 + F1 + F2)**: CI matrix `['3.11', '3.12', '3.13']` doesn't include 3.10 despite `requires-python = ">=3.10"`. The v5.0.1 walker `TestPyprojectClassifiersMatchCIMatrix` only checks 3.14 absent + canonical-set present; doesn't check actual matrix in workflow or assert `min(matrix) == requires-python floor`. **Library claims to support 3.10, but CI does not exercise 3.10.**

**P2-NEW-V2 (V2 only)**: 3 of 5 shim-removal pins lack `match=` migration-recipe text. `lumenairy.ao`, `lumenairy.io.hdf5`, `lumenairy.system` top-level use bare `pytest.raises(ImportError/ModuleNotFoundError)` following the v5.0 template. Future maintainer could re-add a hollow shim that manually raises and still satisfy the contract.

**P2-NEW-F1-3 (F1 only)**: `test_examples_output_dir` source-inspection check too loose. `assert "examples/output" in src or "'output'" in src or '"output"' in src` — second/third disjuncts match ANY 'output' substring. Intent of v4.16.1 centralization no longer enforceable.

**P2-NEW-V4-G (V4 only)**: TestUI3 + TestUI4 in `test_v4_15_agent_e.py:215-216, 254-255` have `@_skip_no_qt` doubled. Functionally harmless (skipif stacks idempotently); sloppy diff applied without re-reading.

**P2-NEW-V4-E (V4 only)**: FAILED test annotations in `unit-tests.yml` use bare `::error::` not `::error file=...::`. Plus greedy grep pattern `"^FAILED |^ERROR |error |Error:"` matches noise (`RuntimeError:`, etc.). Functional but coarse.

### P3 findings (5)

- **P3-NEW-F1-1**: CHANGELOG v5.0.1 entry does not acknowledge the 8 post-tag CI fix commits (convergent with F2 framing).
- **P3-NEW-F1-4**: distortion-grid FP boundary fix still allows cells at `L²+M² = 0.9999` (`N ≈ 0.01`) to silently produce massively distorted results (pre-existing issue swallowed by downstream `except: pass`).
- **P3-NEW-V3-1**: CHANGELOG line-number citations for closures 7+8 slightly off post-edit actual locations (`:932-935 → :929-945`; `:1242-1248 → :1251-1257`) due to TYPE_CHECKING block additions upstream.
- **P3-NEW-F1-2**: tomli fallback path in V11 walker (`test_v4_16_2_dispatcher_pin_doc_consistency.py:51-78`) is now CI-untested (3.10 dropped from matrix).
- **P3-NEW-V4-A-CONCERN**: pyproject.toml `requires-python = ">=3.10"` floor preserved but unenforced.

---

## Part 5 — Yield trajectory + audit-process retrospective (F2)

### Yield

| Release | P0 | P1 |
|---|---|---|
| v4.13.x | 0 | 12-12 |
| v4.14.0-2 | 0 | 4-21 |
| v4.15.0-4 | 0 | 1-14 |
| v4.15.5 | 0 | **0** |
| v4.16.0 mega | 0 | 4 |
| v4.16.0 deep | 0 | 14 |
| v4.16.1 | 0 | 5 |
| v4.16.2 | 0 | 2 |
| v4.16.3 | 0 | **0** |
| v5.0.0 | 0 | 3 |
| **v5.0.1 (declared scope)** | 0 | **0** |
| **v5.0.1 (with v5.0.0 audit miss + 3-way CI finding)** | 0 | **1** |

**Zero physics regressions in 7 consecutive releases**. v5.0.1 declared scope had zero P1 findings — the 1 P1 above is the v5.0.0 audit miss (3-way convergent CI process failure), not a v5.0.1 code defect.

### F2 retrospective: P1 categorization across all 12+ audits in this session

| Category | Approximate count |
|---|---|
| Physics correctness (wrong numerical answer) | ~12 — concentrated in v4.13/v4.14/v4.15.0/v4.16.0 deep |
| Validation / hardening | ~25 |
| Infrastructure (CI / lint / docs / migration) | ~15+ heavily v4.16.x/v5.0.x |
| Sibling-gap meta-pattern itself (V1-V11 fixes) | ~10 |
| Doc / CHANGELOG drift | ~8 |

**Honest retrospective (F2)**:

1. The audit-driven cadence WORKED — surfaced 12+ physics correctness bugs that pre-audit unit tests missed.
2. The V1-V11 dispatcher walker family is the durable artifact — converts "remember to do X at every site" into "test fails if any site forgets X".
3. **Audit value-add is now dominated by infrastructure/doc/CI nitpicks.** v5.0.1's findings are ALL infrastructure (lint baseline, benchmark drift, doc drift, CI process). No physics correctness.
4. The diminishing returns are real but audits remain valuable for: (a) keeping documentation/CHANGELOG honest (long-tail quality); (b) catching ship-discipline failures like the 7-CI-follow-up pattern that nothing else surfaces.
5. **Plausible reduction**: audit cadence could safely move from per-release to per-N-releases or per-month, with a lighter "release-tag verification" workflow filling the gap. The marginal P1 per audit-hour has fallen by an order of magnitude since v4.13.x.

---

## Part 6 — Tier-0 v5.0.2 patch recommendations

### F2's framing: HEAD is de-facto v5.0.2, not v5.0.1

**Concrete recommendations for v5.0.2 ship:**

1. **Bump version**: `pyproject.toml:7` from `5.0.1` to `5.0.2`.

2. **Add `[5.0.2]` CHANGELOG block** describing the 7 CI follow-ups (install fix, validate.yml alignment, artifact upload, GITHUB_STEP_SUMMARY tail, FAILED test annotations, distortion-grid FP boundary, TestUI Qt skips, examples-output-dir refresh) + the 1 library FP-boundary fix (`lumenairy/analysis/field.py`).

3. **Close the Python 3.10 floor sibling-gap** — either:
   - Re-add 3.10 to CI matrix in `unit-tests.yml`, OR
   - Bump `requires-python = ">=3.11"` (and update version classifiers)
   
   Plus harden `TestPyprojectClassifiersMatchCIMatrix` to actually parse `unit-tests.yml` matrix and assert `min(matrix) == requires-python floor`.

4. **Add release-process gate**: one line in `publish.yml` (`needs: [unit, validate]`) plus enable branch protection requiring those checks. This is the structural cause that allowed v5.0.0 + v5.0.1 to ship with broken CI; the 8 commits did NOT deliver this fix.

5. **Update ROADMAP "Current state"** baseline from v5.0.0/2858 to v5.0.2/2895.

6. **3 of 5 shim-removal pins**: add `match="moved.*to.*lumenairy\\."` style migration pointers to bring them to parity with the 2 kwarg/schema pins.

7. **Fix TestUI3 + TestUI4 doubled `@_skip_no_qt`** in `test_v4_15_agent_e.py:215-216, 254-255`.

8. **Tighten `test_examples_output_dir` source-inspection** — require literal `'examples/output/'` substring or `os.path.join(..., 'examples', 'output')` pattern.

9. **Tighten FAILED test annotations**: emit `::error file=...::` instead of bare `::error::`; tighten grep pattern to anchor on test names (`^FAILED tests/...` not just `error |Error:`).

**Aggregate**: ~30 LOC + 4-5 regression tests; ~2 hours of focused work. **v5.0.2 is well-scoped.**

---

## Part 7 — v5.1 horizon (unchanged from v5.0 CHANGELOG)

Per CHANGELOG v5.0.0 lines 101-117 (still active deferrals at v5.0.1):
1. Library-wide consumer wiring for `set_default_wave_propagator` / `set_default_dy` — once landed, v4.16.3 sibling-gap pin fails loudly with actionable cleanup message
2. 6 large-file splits (mechanical, no public API change)
3. 57-file `test_audit_fixes_*` consolidation (mechanical)
4. `lumenairy.MCF` top-level alias (NEW v5.1 polish, NOT the underlying class which shipped v4.15.1)
5. Off-axis conic in surface frame
6. 26 formula-3 glass coefficients ingestion
7. 5 new examples (multi-config/zoom, tolerancing, coronagraph, AO closed-loop, ghost/stray-light)
8. mypy CI activation (after follow_imports cleanup + 63 scope-local fixes)

**Plus from this audit**: ruff promotion out of advisory mode (close the remaining 692 cosmetic errors via mechanical `--fix` PR alongside file splits).

---

## Final verdict

**v5.0.1 IS shippable** — but the honest framing of HEAD is **`v5.0.2`**. The library code touched by v5.0.1 + the 7 CI follow-ups is high-quality: TYPE_CHECKING fixes are textbook-correct (not paper-overs); distortion-grid FP boundary is physically justified (not arbitrary tolerance bumping); warning text + pinning tests genuinely aligned; benchmark fix mathematically equivalent. The shim-removal anti-regression pins for the JAX aperture and `cosmic_ray_rate` kwarg correctly use `match=` to lock in migration-recipe text. The counter-pin in `test_public_api.py` is robust (SIGINT-safe; proves the smoke isn't vacuous). All 11 dispatcher meta-pins green. 34/34 validation pass. F821 regressions caught by a non-advisory unit-test subprocess pin even with lint in advisory mode.

**The single structural concern is release-process discipline.** Three independent agents (V4 + F1 + F2) converged on the unambiguous finding that v5.0.1 was tagged before CI was ever observed green. The 7 CI follow-up commits in <3 hours after the v5.0.1 tag are the evidentiary record. The v5.0.0 audit missed this because verifier audits read the workflow but did not trigger it. **CHANGELOG framing "2889 unit pass / 34/34 validation pass" reflects local results, not CI runs.**

**F2's recommendation stands**: rename HEAD as `v5.0.2`, add a CHANGELOG entry honest about the 7 CI follow-ups, close the Python 3.10 floor sibling-gap, and — most importantly — **add `needs: [unit, validate]` to `publish.yml`** so the next release cannot ship without CI gating.

**Audit retrospective** (F2 verbatim): *"Meta-pattern is structurally retired for code surfaces. Declaring 'meta-pattern narrowed past actionable' is defensible — future occurrences will be infrastructure-class, single-paragraph closures, not multi-cluster releases. Audit value-add is now dominated by infrastructure/doc/CI nitpicks. Plausible reduction: audit cadence could safely move from per-release to per-N-releases or per-month, with a lighter 'release-tag verification' workflow filling the gap."*

**Zero physics regressions in 7 consecutive releases. The library is in mature production-ready state.** Remaining work is bounded, well-scoped, and primarily infrastructure/process rather than core physics.
