# Lumenairy v5.0.0 — Audit Report

**Date:** 2026-05-20
**Library state:** v5.0.0 at HEAD `4334e6a` (v4.16.3 closure at `6b41403` immediately prior).
**Scope:** (a) verify v4.16.3 closes the v4.16.2 audit (2 P1 + 6 P2 + 8 P3 = 16 items); (b) verify v5.0.0 — the FIRST MAJOR breaking-change release — closes its 5 stated breaking changes cleanly and ships the CI infrastructure as documented; (c) exhaustively scan for new bugs, deferred-item integrity, meta-pattern recurrence at v5.0.

**Methodology:** 6 parallel single-domain auditors:
- **V1** v4.16.3 P1+P2 closures (8 items)
- **V2** v4.16.3 P3 closures + V11 walker (`__polynomial__` sentinel parallel structure)
- **V3** v5.0 breaking changes (5 items + Migration-Guide quality)
- **V4** v5.0 CI infrastructure (workflow, ruff, mypy, public-API smoke)
- **F1** Fresh-eyes bug hunt across v4.16.3+v5.0
- **F2** Cross-cutting + 11-pin coverage + v5.1+ deferred-item integrity

v5.0 + v4.16.3 combined: +5160 LOC additions / -157 LOC deletions across 39 files. v5.0 alone is +724 / -412 across 39 files (the breaking-change discipline is real — the library-internal touch is small, dominated by `system.py` move and shim removals).

---

## Part 0 — TL;DR

**v5.0.0 is shippable as a focused breaking-change release** with clean v4.16.2 closure work in v4.16.3 (all 16 items verified). All 5 v5.0 breaking changes properly close with `ModuleNotFoundError` / `ValueError` / `TypeError` raises and correct migration recipes. Migration recipes are mathematically verified correct (V3): aperture `radius=r → diameter=2*r`; `cosmic_ray_rate=R → cosmic_ray_rate_per_m2_per_s=R/A/T`. All 6 large-file splits remain deferred to v5.1+. All 11 dispatcher meta-pins active and green (V1-V11). Test suite: **2858 passed + 5 skipped + 1 xfailed = 2864 collected** — exact CHANGELOG match. **34/34 validation pass.**

**Critical convergent findings (multi-agent independent confirmation):**

| Finding | Streams | Severity |
|---|---|---|
| `set_default_*` UserWarning + pin test actively misleads users at v5.0 HEAD | F1 + F2 (2-way) | **P1** |
| CHANGELOG claims "536 entries" but `len(__all__) == 533` | V3 + V4 + F2 (3-way) | P3 |
| Migration-Guide / CHANGELOG / README all name `simulate_detector_image` but function is `apply_detector` | V3 + F1 (2-way) | P2 |
| `benchmarks/test_bench_jax_jit.py` ships broken on v5.0 (BOTH the system.py move AND the legacy aperture schema break) | V3 + F1 (2-way) | P1 |
| Stale `cosmic_ray_rate` docstring at `detector.py:82-100` ("Retained for back-compat") | V3 + F1 (2-way) | P2 |

**Headline new findings (sorted by severity):**

1. **🔴 P1-NEW-F1-2 (CRITICAL, FORESEEN): `set_default_wave_propagator`/`set_default_dy` UserWarning + pin test ACTIVELY MISLEAD users at v5.0 HEAD** (F1 + F2 2-way convergent). The warning bodies at `propagation.py:381-383, :434-435` say *"Consumer wiring at apply_real_lens / apply_real_lens_traced / propagate is staged for v5.0 alongside the file-split work."* But CHANGELOG.md:108-111 EXPLICITLY DEFERRED that rollout to v5.1+: *"set_default_wave_propagator, set_default_dy, set_default_real_dtype remain API-only at v5.0; the v4.16.3 one-shot UserWarning stays in place."* At v5.0 HEAD the warning tells users "wait for v5.0" — *which IS v5.0*. The pinning test at `test_v4_16_3_agent_b.py:89, :178` enforces `'v5.0' in msg` — **codifying the now-misleading contract**. *v4.16.3's "default-knob honesty" audit-P3-NEW-F1-2 closure is undone by the v5.0 deferral without a text update.* A user calling `set_default_wave_propagator('fresnel')` at v5.0 then `apply_real_lens(...)` silently gets ASM and sees a warning that promises the bug is fixed "in v5.0" — they think they're already on the wired version.

2. **🔴 P1-NEW-V3-1 (CRITICAL): `benchmarks/test_bench_jax_jit.py` ships double-broken on v5.0.** Line 100 imports `from lumenairy.system import propagate_through_system_jax, _PROPAGATE_SYSTEM_JAX_CACHE` → `ModuleNotFoundError`. Line 110 uses `'params': {'radius': 200e-6}` → `ValueError`. **Both v5.0 breaks**. Reproduced by F1. `benchmarks/` not in `tests/unit/` CI collection scope, so unit-CI doesn't catch it.

3. **🔴 P1-NEW-V4-1 (CRITICAL): `ruff check lumenairy/ tests/unit/` baseline FAILS with 696 errors at v5.0 ship.** CI lint job at `unit-tests.yml:58` will BLOCK EVERY PR. Breakdown: I001 400, F401 73, F841 71, F541 69, E702 61, F811 16, F821 4, E401 1, E713 1. **The 4 F821 errors are real code bugs** — forward-reference strings `'Source'`/`'PropagationResult'` in `lumenairy/algebra/base.py` and `lumenairy/propagators/system.py` that should be under `if TYPE_CHECKING:`. Either workflow needs `continue-on-error: true` on lint, the rule set needs narrowing, or a one-shot `ruff --fix` PR must land before this gate becomes meaningful.

4. **🟡 P2-NEW-V3-2 + F1-3 (CONVERGENT): Migration-Guide / CHANGELOG / README all name `simulate_detector_image(..., cosmic_ray_rate=...)` but the actual public function is `apply_detector`.** `simulate_detector_image` is not exported anywhere; zero source matches. Users won't find the function the migration recipe names.

5. **🟡 P2-NEW-F2-1 (FRESH SIBLING-GAP): Shim-removal anti-regression pin coverage is ASYMMETRIC**. Only `lumenairy.analysis.analysis` has `test_analysis_dot_analysis_shim_removed_in_v5_0` at `test_validation_helpers.py:319`. **4 other v5.0 shim removals lack pins**: `lumenairy/ao.py`, `lumenairy/io/hdf5.py`, `lumenairy/system.py` (top-level), JAX aperture legacy schema, `cosmic_ray_rate` kwarg. Risk: v5.1 maintainer accidentally re-adds `lumenairy/ao.py` and no test fails.

6. **🟡 P2-NEW-F2-3: ROADMAP v5.1 section (lines 137-234) is STALE** — lists v5.0-shipped items as still-pending: CI gates, public-API smoke, Python 3.10 bump, system.py move, 5 of 8 shim removals, Migration-Guide existence. "Reads like the v5.0 plan, not the post-v5.0 horizon."

7. **🟡 P2-NEW-F1-4: 2 stale docstring locations.** (a) `lumenairy/analysis/detector.py:82-100` still documents removed `cosmic_ray_rate` as "Retained for back-compat". (b) `lumenairy/propagators/system.py:582` docstring example reads `>>> result = la.system.evaluate(rx, src)` — `la.system` no longer exists.

**Verdict**: v5.0 IS shippable but **lint baseline blocks PR workflow** and the `set_default_*` warning text is **actively misleading at v5.0 HEAD**. F2's recommendation: **v5.0.1 patch (~80 LOC, ~3 hours)** closes all 3 P2s + 2 P3s; v5.1's core mission stays as scoped.

---

## Part 1 — Verification of v4.16.3 closures (V1, V2)

### Agent A — V11 walker + Migration-Guide (V1: VERIFIED ✅)

49/49 tests pass across 4 suites (agent_a 17 + agent_b 16 + agent_c 8 + dispatcher_pin 8).

| Closure | Site | Status |
|---|---|---|
| V11 walker `tomllib` refactor | `test_v4_16_2_dispatcher_pin_doc_consistency.py:42-150`; live-pyproject pins verify `jax-gpu` resolves to `{'jax'}` and `all` includes `refractiveindex` despite embedded `lumenairy[all]` comment | ✅ |
| Migration-Guide §4.16.2 corrected | `Migration-Guide.md:152-206`; explicit "API ONLY in v4.16.2/v4.16.3" annotation; recipe replaced to use only knobs with real consumers | ✅ |
| `get_default_real_dtype` reachable | `ensemble.py:349-368`; canonical `if in_dtype is None: real_dtype = get_default_real_dtype()` path; belt-and-suspenders also in `except (TypeError, ValueError)` | ✅ |
| `set_default_wave_propagator/dy` no-consumer UserWarning + sibling-gap pin | `propagation.py:315-441`; V1 empirically verified ZERO consumers across `lumenairy/`; pin's actionable failure message includes "(a) remove the no-consumer UserWarning... (b) update Migration-Guide §4.16.2..." | ✅ |
| V11 version list CHANGELOG-driven | `_versions_with_breaking_changes_from_changelog()` scans `## [X.Y.Z]` + `### Breaking changes` + body markers; rejects naive bare-`breaking` substring | ✅ |
| V11 Migration-Guide non-triviality | `test_migration_guide_sections_are_non_trivial` enforces ≥200 chars per section | ✅ |
| Constraint auto-probe DeprecationWarning latch | `optimize/core.py:2355, :3287-3311`; pattern-parallel to v4.16.2 MultiWavelengthMerit FutureWarning | ✅ |
| `pickle.dumps` widened to `except Exception` | `optimize/core.py:3261-3276`; explicitly preserves BaseException propagation | ✅ |

### Agent D — P3 cluster (V2: VERIFIED ✅)

15/15 agent_d + 8/8 dispatcher_pin tests pass (walker grew from 7 to 8 with `test_migration_guide_sections_are_non_trivial`).

**`__polynomial__` sentinel is full structural parallel to `__sellmeier__`:**

| Site | `__sellmeier__` | `__polynomial__` |
|---|---|---|
| `get_glass_index` dispatch | `:1042-1049` | `:1062-1070` (line-for-line structural twin) |
| Forward consistency check | `:788-797` | `:798-808` |
| Reverse consistency check | `:809-825` | `:826-843` |
| `get_glass_index_complex` no-extinction | `'__sellmeier__'` | `'__polynomial__'` added at `:1175` |

V2 verified the hypothetical `GLASS_REGISTRY['XYZ'] = '__polynomial__'` + `POLYNOMIAL_COEFFICIENTS['XYZ'] = (c0, [...])` path: passes forward + reverse consistency, dispatches via `_polynomial_index` regardless of `_REFRACTIVEINDEX_AVAILABLE`, returns `n + 0j` from complex path. Behavioral match exact.

Other P3 closures all VERIFIED: dispatch order doc/code reconcile; DEFAULT_* re-exports at top level; per-surface (not max) thickness in high-NA hoist message; multiprocess/fork-safety documentation block; psutil promoted to Required; "structurally retired" softened (one out-of-scope README:334 leftover in v4.16.0 historical section); CHANGELOG `:3015 → :3032` citation refresh verified against live source.

---

## Part 2 — Verification of v5.0 breaking changes (V3)

V3 verdict: **ALL 5 BREAKING CHANGES VERIFIED** with mathematically correct migration recipes. Test suite **2858 + 5 skip + 1 xfail = 2864 collected** exact CHANGELOG match.

| Breaking change | Verification |
|---|---|
| Python 3.10+ floor | `pyproject.toml:11`: `requires-python = ">=3.10"`. `[tool.ruff] target-version = "py310"`, `[tool.mypy] python_version = "3.10"` all coherent. Zero `sys.version_info < (3, 10)` patterns. CI matrix runs 3.10-3.13 (3.14 classifier is aspirational; matrix omits it). |
| `lumenairy.system` → `lumenairy.propagators.system` | Recorded as `git rename`. Public namespace intact: `la.propagate_through_system.__module__ == 'lumenairy.propagators.system'`. `from lumenairy.system import X` raises `ModuleNotFoundError`. **Zero in-tree `from lumenairy.system import` references**. |
| `lumenairy.analysis.analysis` shim removal | `ModuleNotFoundError`. Pinned by `test_analysis_dot_analysis_shim_removed_in_v5_0`. |
| `lumenairy.ao` shim removal | `ModuleNotFoundError`. NO ANTI-REGRESSION PIN (P2-NEW-F2-1). |
| `lumenairy.io.hdf5` shim removal | `ModuleNotFoundError`. NO ANTI-REGRESSION PIN. Minor stale docstring ref at `lumenairy/io/__init__.py:12`. |
| `propagate_through_system_jax` legacy aperture schema | Legacy keys `radius`/`half_width_x`/`inner_radius` raise `ValueError` from `_resolve_aperture_params` at `lumenairy/propagators/system.py:887-934`. 3 sibling tests in `test_audit_fixes_v4_12_0_round4_jax.py:115/149/185` flipped to `pytest.raises(ValueError, match="legacy.*removed in v5\\.0")`. |
| `simulate_detector_image(cosmic_ray_rate=)` kwarg removal | Raises `TypeError: apply_detector() got an unexpected keyword argument 'cosmic_ray_rate'`. Pinned by `TestCosmicRayScaling::test_legacy_kwarg_removed_in_v5_0`. Function name in CHANGELOG/Migration-Guide is wrong — see P2-NEW. |
| `apply_*_lens` shims PRESERVED | `__module__` confirms: `apply_thin_lens.__module__ = 'lumenairy.elements._lens_thin'`; `apply_real_lens.__module__ = 'lumenairy.elements._lens_real'`; `apply_real_lens_traced.__module__ = 'lumenairy.elements._lens_traced'`. |

**Migration recipe math verified correct:**

- **Aperture schema (V3 hand-derivation)**: canonical handler does `r = float(D) / 2.0`; legacy did `r = float(radius)`. To preserve effective `r`: `diameter = 2*r`. Same for `width_x = 2*half_width_x`, `inner_diameter = 2*inner_radius`. ✓
- **`cosmic_ray_rate_per_m2_per_s` (F1 dimensional analysis)**: legacy `R` is a dimensionless COUNT of total strikes (per docstring), NOT a rate. `R/A/T` = `[count]/[m²]/[s]` matches kwarg name. New code: `mean = rate · A · T = [count]`, matches `rng.poisson(mean)` expectation. ✓

---

## Part 3 — Verification of v5.0 CI infrastructure (V4)

V4 verdict: **PARTIAL** — public-API smoke is structural and passes; `ruff check` baseline is BROKEN; `[tool.mypy]` config is latently broken (no CI wiring).

### `tests/unit/test_public_api.py` — VERIFIED structural, not vacuous ✅

V4 demonstrated structural correctness by injecting phantom name `'__lumenairy_phantom_for_audit__'` into `la.__all__`; `hasattr(la, phantom)` correctly returned `False`. Uses `hasattr` (triggers `__getattr__`/lazy resolution) not `name in dir()`. **536 passed in 0.41s.**

### `.github/workflows/unit-tests.yml` — PARTIAL

- Unit job (3.10-3.13 matrix): clean, reproduces 2858/5/1 ✓
- Public-API smoke job: clean ✓
- **Lint job: FAILS** with 696 ruff errors at v5.0 ship (see P1 finding below)

### `[tool.ruff]` config — NOT-WORKING (baseline broken)

Config itself sound (`line-length = 100`, `target-version = "py310"`, `extend-exclude` matches v5.0). But `ruff check lumenairy/ tests/unit/`:

| Code | Count | Description |
|---|---|---|
| I001 | 400 | unsorted-imports |
| F401 | 73 | unused-import |
| F841 | 71 | unused-variable |
| F541 | 69 | f-string-missing-placeholders |
| E702 | 61 | multiple-statements-on-one-line-semicolon |
| F811 | 16 | redefined-while-unused |
| **F821** | **4** | **undefined-name (TYPE_CHECKING forward refs) — REAL BUGS** |
| E401 | 1 | multiple-imports-on-one-line |
| E713 | 1 | not-in-test |

Split: `lumenairy/` = 377 errors; `tests/unit/` = 319. The 4 F821 errors are real code bugs in `lumenairy/algebra/base.py` and `lumenairy/propagators/system.py` — forward-references that should be under `if TYPE_CHECKING:`.

### `[tool.mypy]` config — NOT-WORKING (dormant baseline)

Config itself sound (`python_version = "3.10"`, `strict = true`, `files = ['backend/', '_deprecation.py', '_context.py', 'progress.py', 'memory.py']`). But `mypy --strict <5-module-scope>`:

- **63 errors INSIDE declared scope**: backend/ 49, _deprecation.py 4, _context.py 6, progress.py 3, memory.py 1
- **1889 errors in downstream files mypy follows** (no `follow_imports = silent` or per-file overrides)
- **Currently dormant**: no `mypy` step in `unit-tests.yml`, so not yet blocking. Two fixes needed before activation: (a) clean 63 scope-local errors, (b) add `follow_imports = silent`.

---

## Part 4 — Cross-cutting analysis (F2)

### All 11 dispatcher meta-pins active and green ✅

**259 passed + 2 skipped + 1 xfailed** across all 11 walkers. V11 hardening (`tomllib` parser + CHANGELOG-driven version list + body-marker scan) means new breaking-change releases auto-enroll. v5.0.0 entry auto-detected; Migration-Guide.md has matching `## 5.0.0` section.

### Meta-pattern recurrence: NEW sibling-gap surface at v5.0

| Surface | Auto-pickup? | Risk |
|---|---|---|
| `[tool.ruff] extend-exclude` | Explicit positive-list; new lumenairy/ subdirs auto-linted | Low |
| `[tool.mypy] files` | Hardcoded list; new top-level modules NOT covered | Low |
| CI Python matrix | Hardcoded `3.10-3.13` | Low |
| Public-API smoke | Parametrizes over `la.__all__` — **auto-picks up new entries** | None — best-of-class |
| **Shim-removed status** | Only `analysis.analysis` has pin; 4 others lack pins | **P2 — fresh sibling-gap** |

**The meta-pattern has miniaturized again** — from "function-level → package-level → name-filter → class-method → cache-registry → doc-surface → V11's own internal consistency" to **"shim-removal anti-regression pin asymmetry"** at v5.0.

### Deferred items at HEAD — VERIFIED ✅

All v5.1+ deferrals confirmed intact (F2 verified by direct file inspection):
- 6 large-file splits still monolithic: `raytrace/core.py` 4443; `propagators/propagation.py` 4095; `propagators/asymptotic.py` 4561; `optimize/core.py` 4538; `io/prescriptions.py` 3224; `analysis/core.py` 4088
- Default-config knob consumer wiring: zero consumers library-wide; v4.16.3 sibling-gap pin guards
- `POLYNOMIAL_COEFFICIENTS = {}` empty ✓
- 5 new examples NOT shipped ✓
- 57-file `test_audit_fixes_*` pattern intact ✓
- Off-axis conic NOT shipped ✓

**Note on MCF object**: F2 flags ambiguity in CHANGELOG line 112 "MCF.coherence_at(...) object" — `coherence_at(...)` ALREADY shipped on `PartialCoherenceMCF` in v4.15.1 (`lumenairy/sources/core.py:1598`). It's unclear what's actually deferred: maybe a NEW `MCF` class wrapper or public-API exposure? Worth a v5.0.1 CHANGELOG clarification.

### Test count reconciliation

| Metric | CHANGELOG | Empirical |
|---|---|---|
| Collected | 2864 | **2864** ✓ |
| Pass | 2858 | **2858** ✓ |
| Skip | 5 | **5** ✓ |
| xfail | 1 | **1** ✓ |
| Net delta vs v4.16.3 (2333) | +531 | reconciles ✓ |

**Where does +531 come from?** F2 traced: **almost entirely from `test_public_api.py` (NEW at v5.0): 533 parametrized + 3 standalone = 536 collected tests** in that single file. Other changes net ~-5. CHANGELOG's framing *"v5.0 work landed alongside cumulative v4.16.x test additions in this session"* is **inaccurate** — the +531 is mechanically driven by a single parametrized smoke test, not cumulative v4.16.x backlog.

### Scope discipline ✅

All claimed v5.1+ deferrals hold. No silent shipments verified by walking the 7-item list.

---

## Part 5 — NEW findings (P0/P1/P2/P3)

### P1 findings (3)

**P1-NEW-F1-2/F2-2 — `set_default_*` UserWarning text + pinning test ACTIVELY MISLEAD users at v5.0 HEAD** (F1 + F2 2-way convergent)

- Files: `lumenairy/propagators/propagation.py:378-388, :430-440`; pinning test at `tests/unit/test_v4_16_3_agent_b.py:89, :178`
- Warning bodies: *"Consumer wiring at apply_real_lens / apply_real_lens_traced / propagate is staged for v5.0 alongside the file-split work."*
- CHANGELOG line 108-111 EXPLICITLY DEFERRED rollout to v5.1+
- Pinning test enforces `'v5.0' in msg` — codifying the now-misleading contract
- v4.16.3 audit P3-NEW-F1-2 "default-knob honesty" closure UNDONE by v5.0 deferral without text update
- **Fix**: update warning text "staged for v5.0" → "staged for v5.1" at both sites; update pinning test correspondingly

**P1-NEW-V3-1/F1-1 — `benchmarks/test_bench_jax_jit.py` DOUBLE-broken on v5.0** (V3 + F1 2-way convergent)

- File: `benchmarks/test_bench_jax_jit.py:100, :110`
- Line 100: `from lumenairy.system import propagate_through_system_jax, _PROPAGATE_SYSTEM_JAX_CACHE` → `ModuleNotFoundError: No module named 'lumenairy.system'`
- Line 110: `'params': {'radius': 200e-6}` → `ValueError` (legacy aperture schema)
- Reproduced: `python -m pytest benchmarks/test_bench_jax_jit.py::test_bench_propagate_through_system_jax_first_vs_warm` → `ModuleNotFoundError`
- `benchmarks/` not in `tests/unit/` CI scope so unit-CI doesn't catch
- **Fix**: change to `from lumenairy.propagators.system import ...` and `'params': {'diameter': 400e-6}`

**P1-NEW-V4-1 — `ruff check` lint baseline FAILS with 696 errors at v5.0 ship** (V4)

- File: `.github/workflows/unit-tests.yml:58` runs `ruff check lumenairy/ tests/unit/`
- Live invocation: **696 errors** (377 in lumenairy/, 319 in tests/unit/)
- **4 F821 forward-reference errors are real code bugs** (not config noise) in `lumenairy/algebra/base.py` and `lumenairy/propagators/system.py`
- Every PR will be blocked by lint job
- **Fix options**: (a) workflow `continue-on-error: true` on lint, (b) narrow rule set further, (c) one-shot `ruff --fix` PR before gate becomes meaningful; in any case the 4 F821 forward-refs need real source fixes

### P2 findings (5)

**P2-NEW-V3-2/F1-3 — Migration-Guide / CHANGELOG / README name nonexistent `simulate_detector_image`** (V3 + F1 2-way)

- Files: `Migration-Guide.md:275`, `CHANGELOG.md:50`, `README.md:39`
- All reference `simulate_detector_image(..., cosmic_ray_rate=...)`
- Actual public function is `apply_detector` (`lumenairy/analysis/detector.py:32`); zero source matches for `simulate_detector_image`
- **Fix**: trivial — replace name in 3 doc files

**P2-NEW-F2-1 — Shim-removal anti-regression pin coverage ASYMMETRIC** (F2)

- File: `tests/unit/test_validation_helpers.py:319` has `test_analysis_dot_analysis_shim_removed_in_v5_0`
- **4 other v5.0 shim removals lack pins**: `lumenairy/ao.py`, `lumenairy/io/hdf5.py`, `lumenairy/system.py` (top-level), JAX aperture legacy schema, `cosmic_ray_rate` kwarg
- Risk: v5.1 maintainer accidentally re-adds `lumenairy/ao.py` as shim and no test fails
- **Fix**: 4 more tests modeled on `test_analysis_dot_analysis_shim_removed_in_v5_0` (~30 LOC)

**P2-NEW-F2-3 — ROADMAP v5.1 section (lines 137-234) is STALE** (F2)

- Lists v5.0-shipped items as still-pending: CI gates (line 162), public-API smoke test (line 168), Remove 8 back-compat shims (line 171, but 5 are removed in v5.0), Python 3.10 bump (line 208), system.py move (line 193-195), Migration-Guide existence (line 222)
- F2 verdict: *"Reads like the v5.0 PLAN, not the post-v5.0 horizon."*
- **Fix**: strip shipped items from ROADMAP v5.1 section (~30 LOC delete + reformat)

**P2-NEW-F1-4 — 2 stale docstring locations** (V3 + F1 2-way for detector.py)

- `lumenairy/analysis/detector.py:82-100` still documents removed `cosmic_ray_rate` as `*(4.0+; deprecated 4.9)*` with "Retained for back-compat" — NOT retained
- `lumenairy/propagators/system.py:582` docstring example reads `>>> result = la.system.evaluate(rx, src)` — `la.system` no longer exists
- **Fix**: docstring updates ~10 LOC

**P2-NEW-V4-2 — `mypy --strict` baseline FAILS with 1952 errors (latent)** (V4)

- 63 errors INSIDE declared 5-module scope; 1889 in downstream files mypy follows
- Currently dormant (no CI wiring), but will break the moment activated
- **Fix**: (a) clean 63 scope-local errors, (b) add `follow_imports = silent` and per-file overrides

### P3 findings (8)

- **P3-NEW-V3+V4+F2-1** (3-way convergent): CHANGELOG line 82 cites "536 entries verified at v5.0 ship" but `len(lumenairy.__all__) == 533`. The 536 is the **pytest case count** (533 parametrized + 3 standalone: `test_dunder_all_exists_and_is_a_list`, `test_dunder_all_has_no_duplicates`, `test_version_string_format`). CHANGELOG conflates the two.
- **P3-NEW-F2-2**: CHANGELOG line 72 says ruff excludes `"validation/, docs/, examples/, ui/"` but pyproject.toml correctly scopes as `lumenairy/ui/`. Trivial doc drift.
- **P3-NEW-F1-1**: V11 walker stale Python 3.9 comments at `test_v4_16_2_dispatcher_pin_doc_consistency.py:51-52, :68`. Library is 3.10+ at v5.0; comments need update.
- **P3-NEW-F1-2**: Stale comment in `propagators/system.py:1244-1248` references "one-shot deprecation warning" — v5.0 changed to hard ValueError.
- **P3-NEW-F1-3**: `propagators/system.py:932-935` — unreachable post-`_reject_legacy(...)` code; `r_o`/`r_i` only bound in OTHER branch (UnboundLocalError if reached).
- **P3-NEW-F1-4**: `test_public_api.py` has no negative counter-pin — no synthetic broken-`__all__` fixture proves the assertion machinery works.
- **P3-NEW-F2-MCF**: CHANGELOG line 112 "MCF.coherence_at(...) object" is unclear — `coherence_at(...)` already shipped on `PartialCoherenceMCF` in v4.15.1. What's actually deferred should be clarified.
- **P3-NEW-V3-3**: pyproject classifiers list Python 3.14 but CI matrix runs 3.10-3.13 only. 3.14 entry is aspirational.

---

## Part 6 — Yield trajectory + meta-process

### Yield

| Release | P0 | P1 | P2 |
|---|---|---|---|
| v4.15.5 | 0 | 0 | — |
| v4.16.0 | 0 | 4 | — |
| v4.16.0 deep | 0 | 14 | — |
| v4.16.1 | 0 | 5 | — |
| v4.16.2 | 0 | 2 | — |
| v4.16.3 | 0 | 0 (clean audit follow-up) | — |
| **v5.0.0** | **0** | **3** | **5** |

**Zero P0 / zero physics regressions in 6 consecutive releases.** v5.0's 3 P1s are all in INFRASTRUCTURE (lint baseline, benchmark drift, warning text), not in core code. The deep-audit + verification-audit pattern continues working — each generation surfaces new classes the prior didn't catch, structural counter-measures progressively tighten.

### What v5.0 got STRONGLY right

1. **Scope discipline real** — wider v5.0 plan (file splits, MCF, off-axis conic, formula-3 ingestion, 5 examples, 57-file consolidation) properly deferred to v5.1+. No silent shipments.
2. **Migration recipes mathematically correct** — V3 hand-verified aperture `radius → diameter=2*r` and `cosmic_ray_rate → R/A/T` against actual v5.0 code paths.
3. **Public namespace preserved across `system.py` move** — `la.propagate_through_system` works; only direct private-path imports break.
4. **public-API smoke is structurally sound** — V4 demonstrated by injecting phantom name; uses `hasattr` not `name in dir`.
5. **All 11 dispatcher meta-pins green** — V11 (doc-consistency) auto-detected v5.0.0 entry via CHANGELOG scan.
6. **Migration-Guide.md substantive for v5.0** — concrete old/new recipes for each breaking change.
7. **34/34 validation pass** — physics tests clean across the breaking-change boundary.
8. **`apply_*_lens` preserved as legitimate API surface** — coherent one-stop import not removed despite ROADMAP suggestion; documented rationale.
9. **`__polynomial__` sentinel is structural parallel to `__sellmeier__`** — every consumer site verified by V2.
10. **v4.16.3's sibling-gap pin design** — when v5.1 wires the first consumer of `set_default_wave_propagator`/`dy`, the pin produces an actionable failure message naming exactly what to clean up next.

### What v5.0 needs to improve

1. **CI lint job fails at ship** — workflow needs either `continue-on-error: true` or a one-shot `ruff --fix` PR before lint becomes meaningful.
2. **Stale warning text + pinning test** — undoes v4.16.3's "default-knob honesty" closure.
3. **Asymmetric anti-regression coverage** — only 1 of 5 shim removals pinned.
4. **ROADMAP v5.1 stale** — lists shipped items as pending.
5. **Doc-naming consistency** — `simulate_detector_image` cited but function is `apply_detector`.
6. **Test counts in CHANGELOG conflate `__all__` entries with pytest test count**.

---

## Part 7 — Tier-0 v5.0.1 patch recommendations

**P1 closures (3):**

1. **Update `set_default_*` UserWarning text and pinning test**: change "staged for v5.0" → "staged for v5.1" at `propagation.py:378-388, :430-440` AND `test_v4_16_3_agent_b.py:89, :178` (pinning text). ~10 LOC.

2. **Fix `benchmarks/test_bench_jax_jit.py:100, :110`**: change to `from lumenairy.propagators.system import ...` and `'params': {'diameter': 400e-6}`. ~3 LOC.

3. **Address CI lint gate**: choose one of (a) `continue-on-error: true` on lint job for v5.0 (advisory mode while baseline cleanup is pending), or (b) `ruff --fix` PR landing the 692 auto-fixable; OR (c) split the rule set into "strict" + "advisory" tiers. Either way, fix the 4 F821 real bugs (`lumenairy/algebra/base.py`, `lumenairy/propagators/system.py` forward-refs need `if TYPE_CHECKING:` blocks).

**P2 closures (5):**

4. **Rename `simulate_detector_image` → `apply_detector`** in Migration-Guide.md:275, CHANGELOG.md:50, README.md:39.

5. **Add 4 shim-removal anti-regression tests** modeled on `test_analysis_dot_analysis_shim_removed_in_v5_0`: cover `lumenairy.ao`, `lumenairy.io.hdf5`, `lumenairy.system` (top-level), JAX aperture legacy schema, `cosmic_ray_rate` kwarg. ~30 LOC.

6. **ROADMAP v5.1 section refresh**: strip 8 v5.0-shipped items (CI gates, public-API smoke, 5/8 shim removals, Python 3.10 bump, system.py move, Migration-Guide existence). ~30 LOC delete+reformat.

7. **Fix 2 stale docstrings**: `detector.py:82-100` (remove `cosmic_ray_rate` Parameters block; add v5.0 removal note); `propagators/system.py:582` (`la.system.evaluate` → `la.evaluate`). ~10 LOC.

8. **mypy config preparation**: add `follow_imports = silent` and per-file overrides for non-scope files, OR pin `disable_error_code` for the 63 scope-local errors as a known-deferred starting point. Activation deferred to v5.1 anyway, but config hygiene helps.

**P3 closures (8):**

9. CHANGELOG "536 entries" → "533 entries verified via 536 smoke tests".
10. CHANGELOG "ui/" → "lumenairy/ui/" doc drift.
11. V11 walker stale Python 3.9 comments.
12. `propagators/system.py:1244-1248` stale deprecation-warning comment.
13. `propagators/system.py:932-935` unreachable post-raise code (delete or move under guard).
14. `test_public_api.py` negative counter-pin.
15. CHANGELOG line 112 "MCF.coherence_at(...)" clarification.
16. CI matrix add 3.14 (or drop 3.14 from classifiers — pick one).

**Aggregate**: ~80 LOC + 5-7 regression tests; ~3-4 hours of focused work. **v5.0.1 is well-scoped.**

---

## Part 8 — v5.1 horizon

Per CHANGELOG line 101-117, in priority order:

1. **Library-wide consumer wiring** for `set_default_wave_propagator` / `set_default_dy`. Once landed, the v4.16.3 sibling-gap pin at `test_v4_16_3_agent_b.py:497` FAILS LOUDLY with actionable message ("remove the no-consumer UserWarning + update Migration-Guide §4.16.2 limitation note + update §v5.0 recipe"). Pin is self-documenting.

2. **6 large-file splits** (mechanical; no public API change): `raytrace/core.py`, `propagators/propagation.py`, `propagators/asymptotic.py`, `optimize/core.py`, `io/prescriptions.py`, `analysis/core.py`.

3. **57-file `test_audit_fixes_*` consolidation** (mechanical).

4. **MCF object** — clarify what's actually pending vs `coherence_at(...)` already on `PartialCoherenceMCF` in v4.15.1.

5. **Off-axis conic in surface frame** (feature work).

6. **26 formula-3 glass coefficients** ingestion (data work; 5e-5 n_d cross-check budget may need relaxation for BK7-like polynomial fits per V3 v4.16.2 note).

7. **5 new examples** (multi-config/zoom, tolerancing, coronagraph workflow, AO closed-loop, ghost/stray-light).

8. **mypy CI activation** — after follow_imports cleanup + 63 scope-local fixes.

---

## Final verdict

**v5.0.0 is shippable.** Six consecutive releases with zero P0/zero physics regressions. The breaking-change discipline is real: scope deferred items genuinely deferred; migration recipes hand-verified mathematically correct; Migration-Guide substantive; public namespace preserved; all 11 dispatcher meta-pins green; 34/34 validation pass.

**However, three operational concerns require a quick v5.0.1 patch:**

1. **The lint baseline blocks every PR** — `ruff check` fails with 696 errors at ship; `unit-tests.yml`'s lint job will fail. Choose advisory mode OR cleanup PR.

2. **The `set_default_*` UserWarning text actively misleads users at v5.0 HEAD** — promises "v5.0 will wire consumers" but v5.0 deferred to v5.1+. v4.16.3's "default-knob honesty" audit closure is undone. The pinning test codifies the misleading contract.

3. **The fresh sibling-gap surface** — only 1 of 5 v5.0 shim removals has an anti-regression pin. v5.1 maintainer could accidentally re-add a shim with no test failure.

Plus the broken `benchmarks/test_bench_jax_jit.py` and ~6 doc-drift items.

**F2's framing**: *"The meta-pattern is structurally retired across the surfaces walked by V1-V11. One fresh sibling appeared at v5.0 (shim-removal anti-regression asymmetry). The deep-audit + verification-audit pattern is itself working as designed — each generation surfaces new classes the prior didn't catch."*

**Recommended**: v5.0.1 patch (~80 LOC + 5-7 tests, ~3-4 hours) closes the 3 P1s + 5 P2s + 8 P3s. After that, v5.1's core mission (consumer wiring + 6 file splits + 57-file consolidation) stays as scoped.

The library is in **mature production-ready state**. The audit-driven release cadence continues to deliver structural improvements at successively-narrower scopes; physics correctness has been clean across 6 consecutive releases; meta-pattern gaps are now at the doc/CI/infrastructure level, not at the code level. **v5.0 marks the first production-quality breaking-change milestone of the library.**
