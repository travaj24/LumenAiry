# Consolidated backlog — post v5.14.1 (2026-06-10)

Single source of truth for every OPEN item, consolidated from
`docs/rcwa_roadmap_v5_14.md`, `docs/pmm_roadmap_v5_14.md`,
`docs/audits/ROADMAP_DEVICE_GEOMETRY_SWEEPS_2026_06_10.md` (execution
status), and `docs/audits/AUDIT_FULL_LIBRARY_2026_06_10.md`. Everything
shipped through v5.14.1 has been removed; the per-item evidence lives in the
source docs. **There are no open P1/P2 correctness items** — every confirmed
physics bug found this cycle was fixed before the v5.14.1 tag.

Priority key: ① do next (high value / bounded effort) · ② worthwhile
(medium effort or medium value) · ③ opportunistic / research-grade.

---

## A. Performance

| # | Item | Effort | Notes |
|---|---|---|---|
| ~~① A1~~ | ✅ SHIPPED (backlog batch 1, 2026-06-10; ×4.5 measured on a 4-layer stack) — **RCWA even-parity scope** (`LEV-3`): extend the ×3.9-4.0 symmetry fast path to `RCWAStack.solve`, `rcwa_jones_2d` @ normal incidence, and the per-component tensor operators so the (now-recommended) sequential `'li'` regains it | medium | the gate-and-fallback pattern exists; the fold itself is scalar-only today |
| ~~① A2~~ | ✅ SHIPPED (backlog batch 1, 2026-06-10; ×5.5-6.1 measured) — **PMM 1-D homogeneous-eig share**: one eps-free geometric eig serves both half-spaces (`Mbig(eps) = eps·I − blockdiag(K1, K2)` exactly; shared W, `q²(eps) = q²(1) + Δeps`); 51-64% of 1-D eig time per the PMM audit | medium | needs a presolved-spectrum hook through `_sem_modes_tensor`'s flux selector; derivation pinned in the roadmap |
| ② A3 | `workers=` option on the embarrassingly-parallel field-grid loops (`analysis.image_plane_wfe`, `through_focus`) | small | full-library audit §5 |
| ③ A4 | RCWA `LEV-4/5` micro-wins (analytic homogeneous modes in stacks; diagonal K-matrix assembly) | small | sub-10% each |
| ③ A5 | Maslov polynomial-product loop vectorization | small | degree-bounded; micro |

## B. Capabilities / feature gaps

| # | Item | Effort | Notes |
|---|---|---|---|
| ~~① B1~~ | ✅ SHIPPED (backlog batch 1, 2026-06-10; `by_material=` per-material split deferred, documented) — **PMM2DStack absorption mirror**: `retain_internal` + `layer_absorption(by_material=)` for the 2-D PMM stack (the 1-D version shipped v5.14.1; the 2-D device class will want the same loss maps) | medium | same partial-cascade pattern; per-layer grids make the label mapping simpler than 1-D |
| ~~① B2~~ | ✅ SHIPPED (backlog batch 1, 2026-06-10) — **`PMMStack.internal_field(z)`**: the stored modal amplitudes already support field reconstruction (absorption shipped; the field accessor didn't) | small-medium | device-geometry item 6's second half |
| ② B3 | **`fff_nv` rework-or-retire decision**: post-fix it beats `'li'` nowhere measured; either make the NV smoothing width physical + validate a curved-wall cross term, or deprecate toward `'li'`/shapes | decision + small | RCWA audit F2 |
| ② B4 | **`coat(where='tops|walls|floor')` subset filters** for `SegmentStackGeometry` — only if an application actually needs them (the build-order idiom covered the real device) | medium | deliberate v1 exclusion |
| ② B5 | **RCWAStack OOP `retain_internal`**: internal fields/absorption for stacks with out-of-plane layers (currently raises) | medium | needs the generalized-cascade partial-S recovery |
| ② B6 | **JAX guards on the remaining sweep wrappers** (the 1-D efficiency sweep got the loud vmap-pointer reject; the jones/segments/2-D sweep wrappers still silently materialize) | small | audit SWEEP-JAX, partially done |
| ② B7 | **Zemax COORDBRK load-time warning** (tilted/decentred prescriptions import as their on-axis skeleton with only a docstring note today) | small | full-library audit P3 |
| ③ B8 | **Native 2-D PMM slant** (Edée & Granet 2024, in PMM_Papers) — unlocks 2-D covariant tapers too | large | PMM roadmap #5 |
| ③ B9 | **Native exact trapezoid-metric PMM layer** — the roadmap sketch was REFUTED (1/w(z)² z-dependence breaks constant-coefficient); honest starts: exponential-taper gauge or Magnus z-ODE | research | staircase + snap + slices-consensus remain the path |
| ③ B10 | **Li-1997 per-direction mixed rules for 2-D tensor diagonals** (PMM roadmap #6; the RCWA scalar version shipped) | medium | accuracy, anisotropic 2-D cells |
| ③ B11 | **Mueller-matrix calculus** (depolarizing elements; Stokes/DoP exist) | medium | niche for current applications |
| ③ B12 | **Tilted/decentred wave-optics chain** (3-D raytrace world exists; the scalar-diffraction chain is axis-aligned) | large | pairs with B7 |
| ③ B13 | **Partial-coherence propagation** beyond Schell-model sources (mode decomposition) | large | coherence module is thin (222 LOC) |
| ③ B14 | RCWA `GAP4` µ/bianisotropic media; `GAP6` hex lattices + parallelogrammic truncation | research | |
| ③ B15 | JAX twin for PMM slanted-Jones / out-of-plane (Ph5, deferred since the JAX-differentiability cycle) | large | host-side argsort blocks tracing |

## C. Hygiene / standardization (all small)

| # | Item | Notes |
|---|---|---|
| ② C1 | Standardize `verbose=False` defaults (census: 9 True / 12 False) and route the 99 gated prints through `lumenairy._logging` | full-library audit §4 |
| ② C2 | Replace the sandboxed `eval` in `user_library.load_phase_mask` with a small AST evaluator (sandbox is sound; this removes the class) | tracked in-code since 4.10 |
| ② C3 | "Which lens model do I want" doc table for the 5-tier lens ladder (thin → real → traced → JAX → Maslov) | discoverability |
| ③ C4 | Split `sources/core.py` (3.0k LOC, last monolith) along the v5.1-style verbatim-slice pattern | |
| ③ C5 | Spin `ui/` (24% of LOC) into a `lumenairy-designer` companion package | import-isolated already; identity/packaging nicety |
| ③ C6 | `_require_inplane_tensor` docstring still names "2-D / RCWAStack out-of-plane … pending" — stale after v5.14.1 | one line |

## D. Test / release infrastructure

| # | Item | Notes |
|---|---|---|
| ~~① D1~~ | ✅ SHIPPED (backlog batch 1, 2026-06-10: docs/TOLERANCE_POLICY.md + tests/unit/_tolerances.py) — **Written BLAS-gauge tolerance policy** (degenerate-pair gauge contracts: when bit-identity is allowed, when 5e-6/1e-9-class physical tolerances are mandatory). Four runner-flavored re-pins were needed this cycle alone; a policy doc + a `tests/unit/_tolerances.py` constants module would end the churn | recurring cost |
| ② D2 | **Pre-tag fresh-clone smoke step** in the release flow (open since the v5.4 CI lessons; three force-retags that release) | small CI job |
| ② D3 | Module-level `importorskip` lint walker (the disease that silently killed 140 tests on CI; fixed for 5 files — a walker prevents recurrence) | small |
| ③ D4 | CuPy≥14 numerical gate for `cupy.linalg.eig` vs numpy on a real CUDA box (pin landed; the numerical comparison awaits hardware) | hardware-gated |
| ③ D5 | Wood-anomaly / large-period instability: a documented "known-erratic geometry" fixture family so probes/tests stop rediscovering it | docs/tests |

## E. Application-side (tracked here, owned there)

- Re-pin `pbs_qwp_mirror_sim` references per staircase realization (its
  acceptance doc's own advice) and retire `coated_jones_rcwa`'s hand
  pixelation now that `to_rcwa_stack` takes tensors (v5.14.1).
- Adopt `reflective_outcoupling` for the QWP/PBS chain (feedback doc, in
  progress on the application side).
