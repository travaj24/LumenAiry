"""Validation tests for the lumenairy library.

Scripts in this folder compare library output against independent
references (geometric ray trace, analytic formulas, Zemax) and produce
numerical + visual reports.  These are NOT pytest unit tests -- they
are standalone scripts meant to be run interactively when you want to
verify that a physics routine matches ground truth.
"""
